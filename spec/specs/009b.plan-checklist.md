# Companion Checklist — v0.5.3 VPN / Jaringan Khusus / Domain Kustom

- **Spec**: [`009.plan-v0.5.3-network-vpn-custom-domain.md`](./009.plan-v0.5.3-network-vpn-custom-domain.md) (Implemented 2026-08-23)
- **Status**: Implemented — semua gate automatis hijau (147/0); 2 item manual smoke ditandai user-side
- **TDD**: Red → Green per milestone; gate `cargo test` + `clippy -D warnings` + `fmt --check` + `check_budget.sh` + `graphify update .`
- **Prinsip**: perluasan akses tidak menyentuh auth/pairing/CSRF; tanpa flag baru = perilaku identik (regresi nol)

---

## 0. Ringkasan Deliverable

| Area | File | Perubahan |
|---|---|---|
| Config | `src/config.rs` | `allowed_hosts`, `advertise_addrs` + parser/validator + `is_cgnat()` helper |
| Gate | `src/http.rs:292` | `host_origin_middleware` cek allowlist + CGNAT; pesan `HOST_REJECTED` baru |
| Banner | `src/main.rs:24,159-194` | `collect_access_urls` murni + cetak advertise/CGNAT |
| Tests | `tests/security_tests.rs` (+ parser tests) | Red list di bawah |
| Docs | clap `after_help`, `tests/traceability.md` | 3 contoh skenario; `NET-001..007` |

---

## 1. Milestone A — Parser & Gate (`NET-001`, `NET-002`, `NET-006`, `NET-007`)

### Red (parser — field belum ada → compile error = RED)
- [x] Red: tabel kasus valid — `a.test`, `A.Test`, `host.tail-scale.ts.net`, `100.64.1.2`, `mux.lab.internal` (dengan trim spasi).
- [x] Red: tabel kasus invalid — `*`, `*.ts.net`, `http://a.test`, `a.test/x`, `a b`, `''` (entri kosong), `a..test` → pesan `invalid --allowed-hosts entry #N` + exit 1.

### Red (gate — `security_tests.rs`)
- [x] Red: `test_net_001_allowed_host_accepted` — server dengan `allowed_hosts=["allowed.test"]`; request `Host: allowed.test` (dengan & tanpa port) → **bukan** 403.
- [x] Red: `test_net_001_case_insensitive` — `Host: ALLOWED.TEST` → lolos.
- [x] Red: `test_net_007_evil_host_still_rejected` — `Host: evil.com` → 403 `HOST_REJECTED` (regresi keamanan).
- [x] Red: `test_net_002_cgnat_ip_accepted` — `Host: 100.64.1.2:port` → lolos **dengan** helper baru.
- [x] Revisi faktual (log D5): NET-002 mengubah perilaku CGNAT → dikunci `test_net_002_cgnat_ip_accepted` (accepted); publik dikunci `test_net_007` (evil.com, 93.184.216.34 → 403).

### Impl backend
- [x] `src/config.rs` — `allowed_hosts: Option<String>` (flag CSV + env) → `effective_allowed_hosts(): Vec<String>` (lowercase, validasi `NET-006`, fail-fast).
- [x] Helper IP: `fn is_gate_ok_ip(ip, effective_bind) -> bool` = private \|\| CGNAT(100.64/10) \|\| loopback \|\| == bind; dipakai gate & banner.
- [x] `src/http.rs` — `host_ok` diperluas: IP via helper; domain via allowlist; port check tetap.
- [x] Wiring `main.rs` (fail-fast) + helper lokal `start_server_with_hosts` di security_tests (lebih ringkas daripada ubah common).

### Green
- [x] `cargo test` **100%** (147/0; existing tanpa edit) • clippy 0 • fmt OK.

---

## 2. Milestone B — Advertise & Banner (`NET-003`, `NET-004`, `NET-005`)

### Red
- [x] Red: `test_net_003_advertise_implies_allowlist` — server dengan `advertise_addrs=["a.test:7681"]`, request `Host: a.test` → lolos.
- [x] Red: unit `collect_access_urls(scheme, port, bind, ips, advertises) -> Vec<String>` — memuat `127.0.0.1`, advertise, CGNAT IP; **dedupe**; urutan loopback→advertise→lan (`net_004_collect_access_urls_order_and_dedupe`).
- [x] Red: `HOST_REJECTED` message mengandung `--allowed-hosts` hint (NET-005) (`test_net_005_host_rejected_message_hints_flag`).

### Impl
- [x] `src/config.rs` — `advertise_addrs: Option<String>` + validasi `host[:port]`; segmen CSV kosong di-skip (bugfix: `Some("")` pernah menghapus allowlist via `unwrap_or_default`).
- [x] `src/main.rs` — banner via `collect_access_urls` (loopback→advertise→LAN/CGNAT, dedupe); non-LAN tetap cetak `VPN/Custom:`; `private_ips` + CGNAT.
- [x] `src/http.rs` — pesan `HOST_REJECTED`: `"invalid Host header — add it via --allowed-hosts"` (tanpa echo Host).

### Green
- [x] `cargo test` 100% • clippy/fmt 0.
- [ ] Manual smoke Tailscale/MagicDNS — user-side device (logika terkunci unit `net_004_collect_access_urls_order_and_dedupe`).

---

## 3. Milestone C — Docs & Traceability (`NET-004`, `NET-007`)

- [x] clap `after_help`: 4 contoh — Tailscale (`--lan --advertise-addr host.ts.net:7681`), WireGuard isolate (`--bind 10.8.0.2`), DNS internal (`--allowed-hosts mux.lab.internal`).
- [x] `tests/traceability.md`: `NET-001..007` → **Passed** (regresi SEC/AUTH tetap Passed).
- [x] `graphify update .` sinkron; handoff 006 dibuat.

---

## 4. Quality Gates & DoD

- [x] `cargo test` 100%: **147/0** • clippy 0 • fmt 0.
- [x] `./scripts/check_budget.sh` PASS (frontend tak berubah).
- [x] Versi → `0.5.3` (Cargo.toml + web/package.json).
- [ ] **Manual E2E user-side**: akses via VPN mesh (IP + MagicDNS) lolos pairing; host asing → 403; tanpa VPN perilaku lama.

---

## 5. Out of Scope (ingat)

Wildcard allowlist, reverse-proxy/`X-Forwarded-*`/`--trusted-proxies`, ACME, mDNS, perubahan auth — spec §9.

---

## 6. Keputusan & Open Question Log

| # | Keputusan / Question | Status |
|---|---|---|
| D1 | Exact-match only; wildcard ditunda | Decided (§8.1) |
| D2 | CGNAT 100.64/10 = kelas private gate+banner | Decided (§8.2) |
| D3 | Advertise implisit masuk allowlist | Decided (§8.3) |
| D4 | Abaikan `X-Forwarded-*` di MVP | Decided (§8.4) |
| Q1 | Advertise tanpa port → pakai `config.port` | Open (default ya) |
| Q2 | IPv6 zone-id di Host → tolak di MVP | Open (default tolak) |
| A1 | ⚠️ Versi rilis disebut **v0.5.3** (pola patch 0.5.x) | Asumsi — konfirmasi developer |
