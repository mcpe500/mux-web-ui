# Companion Checklist — v0.6.5 Update Command `mux-web update` (opsi A)

- **Spec**: [`015.plan-v0.6.5-update-command.md`](./015.plan-v0.6.5-update-command.md)
- **Status**: Implemented (2026-08-27) — all gates PASS
- **TDD**: Red → Green; gates `cargo test` + `clippy -D warnings` + `fmt --check` + `vitest` + `tsc --noEmit` + `check_budget.sh`

## 1. UPD Milestone

### Red
- [x] Cargo: `Config::try_parse_from(["mux-web","update"])` → `Commands::Update` ; `["mux-web","update","--check"]` → check=true ; `--url` custom allowlisted vs evil ditolak; `is_update_url_allowed` table. → `tests/update_cmd_tests.rs:6` PASS

### Impl
- [x] `src/config.rs` `Commands` enum optional subcommand + `MUX_WEB_UPDATE_URL` env default const. → `src/config.rs:121` `#[command(subcommand)] pub command` + `enum Commands::Update{check,url}` PASS
- [x] `src/update.rs` `is_update_url_allowed` allowlist + `handle_update(check, url_opt)` → if check fetch LATEST via curl API else spawn `curl -fsSL url | bash` inherit. → `src/update.rs:13` `ALLOWED_PREFIXES` + `handle_update` PASS
- [x] `src/main.rs` early dispatch `if let Some(Commands::Update{..}) { handle_update().await; return Ok(()) }` sebelum bind. → `src/main.rs:101-116` early return PASS

### Green
- [x] `mux-web --help` contains `update`; `mux-web update --help` contains `--check`. → `cargo run -- --help` `Commands: update` + `cargo run -- update --help` `Options: --check --url` PASS
- [x] Non-allowlist `--url https://evil.com/x.sh` exit 1 pre-curl; src test covers. → `cargo run -- update --url https://evil.com/payload.sh` `INVALID_URL` exit 1 PASS + `update::tests::upd_004_allowlist_table` PASS
- [x] Gates hijau (`cargo test 231` `clippy 0` `fmt 0` `vitest 280` `tsc 0` `budget 537 KiB`), smoke S13.

## 2. Docs
- [x] README update command snippet; supportLogic guide ke-10 (`supportLogic.ts:167` `id:update` 4 steps).
- [x] smoke S13: `mux-web update --check` (`Versi lokal v0.6.5 lebih baru dari rilis terbaru v0.6.1`) + `mux-web update --url file:///tmp/fake-install.sh` (`dummy update ran` stream) di `spec/handoff/smoke-v0.6.3-device-checklist.md`.
- [x] Versi → 0.6.5 (`Cargo.toml:3` `0.6.5`, `web/package.json:4` `0.6.5`), handoff 011 pending (this checklist covers traceability).
