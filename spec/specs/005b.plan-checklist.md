# Companion Checklist — v0.5 Workspace, Desktop & File Manager Sempurna (Windows Explorer + VS Code + Browser) — Lebih Baik & Tidak Ngelag

- **Spec**: [`005.plan-v0.5-workspace-desktop-filemanager.md`](./005.plan-v0.5-workspace-desktop-filemanager.md) (sempurna — 2026-08-21 Build)
- **Status**: proposed → **approved** (perfect recreation, better & no-lag — tunggu `Status: approved` final setelah user `Setuju`)
- **TDD**: Red → Green per sub-fitur, `clippy -D warnings` 0, `fmt` 0, `graphify update .` tiap fase, **perf test wajib** (virtualized, debounce, LRU)
- **Keputusan Final (dari §9):** WS Single-folder `localStorage` + server fallback, Desktop icon ya (drag-drop + widget taskbar), Explorer Windows 11, Editor VS Code (prism lazy), Browser iframe tabs — semua **lebih baik** dari native karena terintegrasi & ringan

---

## 0. Keputusan Desain Final (Sudah Dijawab — Konfirmasi)

- [x] `WS model`: **Single-folder** (1 `root+path`/workspace, Multi-root tunda `v0.5.1`) — simple, cocok Termux 1 project
- [x] `Persist`: **`localStorage["mux_workspace_current"]` + `localStorage["mux_recent_workspaces"]` (10) + server `~/.mux-web/workspaces.json` fallback `MUX_WEB_WORKSPACES_FILE`** — sinkron per-browser, server optional
- [x] `Desktop icon`: **ya** — `DesktopIcons.tsx` `x,y` per-workspace `mux_desktop_icons_${wsId}` max 100, drag `PointerEvents` + `RAF`
- [x] `Widget`: **taskbar** `GitWidget` poll 10s + desktop widget pinned opsional (toggle Settings)
- [x] `FM style`: **Windows 11 Explorer sempurna** `FM-001..018` — Navigation Pane, Ribbon, Breadcrumb, Search, View Modes 3, Sorting, Selection, Drag-drop, Context 9 verbs, Preview, Status Bar, History, Favorites
- [x] `Editor style`: **VS Code sempurna** `ED-001..010` — Tabs, Tree, Syntax `prismjs` lazy 15 KiB, Find/Replace, Minimap, Split, Integrated Terminal, Command Palette
- [x] `Browser`: **Chrome-like inside** `BR-001..006` — Tabs, Omnibox, Nav, Bookmarks Bar, History, Sandbox
- [x] `Performance`: **Tidak ngelag** — virtualized `Grid` 10k <16ms, search debounce 300ms + Worker, preview LRU 50, highlight lazy, widget poll 10s, budget 750 KiB

---

## 1. Workspace System — Sempurna (`WS-001..008`)

### Red Tests (`tests/workspace_tests.rs`)
- [ ] `test_ws_001_create_rejects_traversal` — `POST /workspaces {"path":"../../etc"}` → `400 PATH_TRAVERSAL` (`AllowedRoots::resolve_path` `src/paths.rs:63`)
- [ ] `test_ws_002_create_rejects_nul` — `{"path":"a\0b"}` → `400 NUL`
- [ ] `test_ws_003_current_persists` — `POST` `myws` `home:/work/app` → `201`, `PUT /current {id}` → `200`, `GET /current` → sama + `localStorage` reload `mux_workspace_current` masih `myws`
- [ ] `test_ws_004_discover_bounded` — `GET /git/repos?root=home&path=&depth=5&limit=50` pada `home` dengan 10k file + `node_modules/.git` skips → `<200ms`, `len ≤50`, tidak escape `AllowedRoots`
- [ ] `test_ws_005_recent_limit_10` — buat 11 workspace → `GET /workspaces` `len 10` (max 20, recent 10)
- [ ] `test_ws_006_set_from_file_explorer` — `WorkspaceSelector` `Set as Workspace` `home:/work` → `GitView` top-bar `rootId` update tanpa reload (vitest `WorkspaceSelector.test.tsx`)
- [ ] `test_ws_007_name_validation` — `name="a; rm -rf"` → `400` regex `^[a-zA-Z0-9 _.-]+$`
- [ ] `PERF` `test_ws_perf_discover_10k_no_lag` — 10k file tree walk <200ms, no OOM

### Impl
- [ ] `src/workspace.rs` `WorkspaceRegistry {workspaces: HashMap<Id, Workspace>, current: Option<Id>}` + `max_workspaces 20` + `AllowedRoots` + `GET/POST/PUT /api/v1/workspaces` + `GET /git/repos` bounded
- [ ] `web/src/apps/workspace/WorkspaceSelector.tsx` dropdown `Recent` + `Create` modal + `Set as Workspace` `localStorage` + `fetch` `workspaces/current`
- [ ] `web/src/desktop/DesktopCanvas.tsx:12` baca `mux_workspace_current` untuk `openGit/openTerminal` default

### Green
- [ ] `cargo test --test workspace_tests` **8/8** + `vitest` `WorkspaceSelector.test.tsx` 3/3
- [ ] `cargo clippy` 0, `graphify update .` cohesion naik

---

## 2. Desktop System — Sempurna (`DESK-001..008`) — Lebih Baik dari Windows

### Red Tests
- [ ] `test_desk_001_icon_traversal_rejected` — `POST /desktop/icons {"path":"../../../../.ssh/id_rsa"}` → `400`
- [ ] `test_desk_001_icon_drag_persist` — `DesktopIcons` drag `x 100→200` + reload → `localStorage["mux_desktop_icons_${wsId}"]` `x 200` (vitest)
- [ ] `test_desk_003_snap_grid` — `WindowFrame` drag `x 5` snap to `8px` grid, `MIN_WIDTH 240` `WindowFrame.tsx:5`
- [ ] `test_desk_004_start_menu_search` — `Ctrl+K` palette type `term` → `Terminal` result <50ms
- [ ] `test_desk_005_widget_poll_no_flood` — `GitWidget` `setInterval 10s` tidak `setTimeout 100ms` (spy `fetch` count `1` in 11s, not `100`)
- [ ] `test_desk_006_layout_per_workspace` — switch `wsA->wsB` → `localStorage["mux_layout_wsA"]` save/restore `WindowState[]`
- [ ] `test_desk_007_wallpaper_persist` — `localStorage["mux_wallpaper"]` `slate` reload still `slate`

### Impl
- [ ] `web/src/desktop/DesktopIcons.tsx` `icon {id, root, path, x, y, icon}` `absolute` `PointerEvents` + `RAF` + `onDoubleClick` → `files/editor/browser`
- [ ] `web/src/desktop/Wallpaper.tsx` + `web/src/styles/main.css` `background` enum
- [ ] `web/src/apps/git/GitWidget.tsx` taskbar mini `branch/dirty` poll `10s` `fetch /api/v1/git/status?workspace=current` + toggle `Settings`
- [ ] `web/src/desktop/CommandPalette.tsx` `Ctrl+K` search apps+recent+workspaces
- [ ] `windowStore.ts:1` key `mux_layout_${workspaceId}` + `mux_desktop_icons_${wsId}`

### Green
- [ ] `vitest run` `windowStore.test.ts:1` 3/3 + `DesktopIcons.test.ts` 2/2 + `GitWidget.test.ts` 1/1
- [ ] `cargo test` `DESK` perf no flood

---

## 3. File Manager Windows-like — Sempurna (`FM-001..018`) — Recreation + Lebih Baik

> **Sempurna = semua yang ada di Windows 11 Explorer ada di Mux, plus lebih baik (integrasi workspace, cepat).**

### Red Tests (`tests/fs_tests.rs` + `web/src/apps/files/FileExplorerView.test.tsx`)
- [ ] `test_fm_001_navigation_pane` — `GET /fs/roots` `home/cwd` + `Quick Access` `Favorites` + `This PC` `Drives` + Tree expand depth 1 lazy (vitest click `📁` → `loadDir` called)
- [ ] `test_fm_002_ribbon_toolbar` — `New File` `New Folder` `View` `List/Grid/Details` buttons exist (vitest)
- [ ] `test_fm_003_address_breadcrumb` — breadcrumb `Root › work › app` segment click → `navigateTo('/work')` (vitest)
- [ ] `test_fm_004_view_modes_sort` — `Details` header click `Name ▼` → `▲` + `Grid` `auto-fill 110px` `259` visible 30/1000 (perf)
- [ ] `test_fm_005_search_scoped` — `GET /fs/search?root=home&path=&q=a` → `400` (min2), `q=hello` → `200` `len ≤100`, `q=../` → `400`, debounce 300ms (vitest + `fs_tests`)
- [ ] `test_fm_006_create_rename` — `PUT /fs/file?root=home&path=/new.txt` `201` + `Rename` `F2` inline `PATCH` → `200`
- [ ] `test_fm_007_copy_move_progress` — `POST /fs/actions` `Copy` folder 100 file → `WS /fs/progress/{id}` `0→100` without OOM (stream 8 KiB `src/archive.rs:276` pattern)
- [ ] `test_fm_008_trash_vs_permanent` — `DELETE` no `confirm` → `Trash` (`~/.trash` move), `confirm=true` → permanent `204`
- [ ] `test_fm_009_properties` — `GET /fs/stat?root=home&path=/a.txt` → `{size,mtime,mode}` modal
- [ ] `test_fm_010_preview_pane` — select `a.txt` <200 KiB → `fetchPreview` `123` `Loading...` → `Hello` <50ms from LRU, `big.png` 5MB → `[Binary]` (lazy)
- [ ] `test_fm_011_status_bar` — `12 items • 2 selected • filtered "foo" • Details` bottom `330`
- [ ] `test_fm_012_selection_model` — `Ctrl+Click` toggle, `Shift+Click` range, `Ctrl+A` all (vitest)
- [ ] `test_fm_013_drag_drop` — `PointerEvents` drag ghost + drop target highlight + `onDrop` → `Copy/Move` (vitest)
- [ ] `test_fm_014_context_open_in` — right-click `file.txt` → `Open/Open in Terminal/Git/Browser/Package/Archive/Share` 9 verbs `336-364` (vitest click `Open in Git` → `onOpenInGit` called)
- [ ] `test_fm_015_history` — `← → ↑` `history[]` `88` stack max 50, `Backspace` Up
- [ ] `test_fm_016_favorites_pin` — `☆ Pin` → `localStorage["mux_favorites"]` `["home:/work"]` + sidebar `Favorites (1)`
- [ ] `test_fm_017_keyboard_shortcuts` — `F2` rename, `F5` refresh, `Delete` trash, `Shift+Del` permanent, `Ctrl+C/V` (vitest `keydown`)
- [ ] `test_fm_018_quick_access` — pin `home:/work` → Quick Access section `Favorites` `199`
- [ ] `PERF` `test_fm_virt_10k` — `filteredEntries` 10k virtualized scroll 60fps <16ms/frame (vitest `performance.now`)

### Impl
- [ ] `src/files.rs` `search(q, root, path, limit, depth)` bounded `max_depth 5` `max_results 100` regex `^[a-zA-Z0-9+._ -]*$` + `stat` + `progress` WS
- [ ] `src/http.rs:64` new `GET /api/v1/fs/search` `GET /api/v1/fs/stat` `GET /api/v1/fs/tree?depth=1` + `POST /fs/actions` wiring
- [ ] `web/src/apps/files/FileExplorerView.tsx:19` windows-like full: nav pane `194-216`, ribbon `220-235`, breadcrumb `237-252`, grid/details `259-304`, preview `308-327`, status `330-334`, context `336-364` (already) + virtualized (`@tanstack/virtual` light or manual slice), column resize, grouping future

### Green
- [ ] `cargo test --test fs_tests` 18/18 + `vitest` `FileExplorerView.test.tsx` 10/10
- [ ] `cargo clippy` 0, `npm run build` still ≤750 KiB (grid virtualized keeps DOM 30, not 10k)

---

## 3a. Editor VS Code-like — Sempurna (`ED-001..010`) — Lebih Baik & Tidak Ngelag

### Red Tests (`web/src/apps/editor/*.test.tsx`)
- [ ] `test_ed_001_tabs_dirty` — `TabData` `TextEditorView.tsx:23` `●` `392`, `Ctrl+Tab` focus, `Close Others` (vitest)
- [ ] `test_ed_002_syntax_highlight` — `main.rs` `fn` keyword span `color #569cd6` via `prismjs` lazy `ED-002` (vitest)
- [ ] `test_ed_003_find_replace` — `Ctrl+F` bar `showFind` `436`, `findQuery="hello"` → `Found 3 matches` `232`, `Replace All` `240` (vitest)
- [ ] `test_ed_004_minimap_outline` — minimap `64px` `494` slice `3000`/`100` + outline `functions/classes` regex (vitest)
- [ ] `test_ed_005_bracket_indent` — `Tab` → `2 spaces` `197`, `Enter` keep indent, `()` auto-close (vitest)
- [ ] `test_ed_006_split_editor` — `Ctrl+\` → `BATCH_ADD_WINDOWS` `windowStore.ts:73` tile second editor (vitest)
- [ ] `test_ed_007_integrated_terminal` — `Ctrl+`` `showTerminal` `502` placeholder + `TerminalView` reuse (vitest `showTerminal` toggle)
- [ ] `test_ed_008_command_palette` — `Ctrl+P` quick open `customPathInput` `333` + `Ctrl+Shift+P` palette (vitest)
- [ ] `test_ed_009_explorer_outline` — sidebar `renderTree` `263` + `Outline` tab (vitest)
- [ ] `test_ed_010_theme` — `localStorage["mux_editor_theme"]` `dark` toggle (vitest)
- [ ] `PERF` `test_ed_perf_5k_highlight` — `prism` highlight 5k line <100ms (vitest `performance.now`)

### Impl
- [ ] `web/src/apps/editor/TextEditorView.tsx:36` polish: `prismjs` `import('prismjs/components/prism-rust')` lazy per ext, overlay highlight `div` behind `textarea`, `doFind` `232` + `doReplace`, minimap `494`, terminal split `502`, toolbar `Find/Terminal/Highlight` `436`, `handleKeyDown` `Ctrl+F/H/`/``
- [ ] `web/package.json:6` add `prismjs@1.29` 15 KiB (lazy chunk, not main)
- [ ] Keep `textarea` for edit + `pre` for highlight toggle `highlight` `436` — budget safe vs Monaco

### Green
- [ ] `vitest` `editor.test.ts` 10/10 + `npm run build` still `92 KiB → ~110 KiB` gzip (<750)

---

## 3b. Browser Inside Desktop — Sempurna (`BR-001..006`) — Chrome-like

### Red Tests (`web/src/apps/browser/*.test.tsx`)
- [ ] `test_br_001_iframe_sandbox` — `BrowserView.tsx:1` `iframe sandbox="allow-scripts allow-same-origin allow-forms allow-popups"` without `allow-top-navigation` (vitest attr)
- [ ] `test_br_002_tabs` — `Tab[]` `BrowserView.tsx:4` `+ New Tab` `addTab` + `✕` close persist `localStorage["mux_browser_tabs"]` reload still 2 tabs (vitest)
- [ ] `test_br_003_omnibox_navigation` — input `example.com` Enter → `navigate` prefix `https://` + `history` push + `← →` `history.back` (vitest)
- [ ] `test_br_004_bookmarks_bar` — `☆` toggle → `localStorage["mux_bookmarks"]` `["https://example.com"]` + bar `flex` `29` (vitest)
- [ ] `test_br_005_history` — `history[]` limit 100 `BrowserView.tsx:10` + `Ctrl+H` (vitest)
- [ ] `test_br_006_find_zoom` — `Ctrl+F` `iframe.contentWindow.find` + `Ctrl+Wheel` `scale` (vitest)

### Impl
- [ ] `web/src/apps/browser/BrowserView.tsx:1` chrome-like already: tabs `4`, nav `← → ↻` `30`, omnibox `input` `Go`, bookmarks `8`, history `10`, iframe `sandbox` — polish: favicon `new URL(url).hostname` `11`, zoom, find
- [ ] `web/src/desktop/DesktopCanvas.tsx:192` `openBrowser(url)` + `BrowserView` `win.appId==='browser'` `242` + taskbar `🌐 Browser` `278`

### Green
- [ ] `vitest` `browser.test.ts` 6/6 + `npm run build` `~5 KiB` extra (<750)

---

## 4. DoD & Quality Gates — Sempurna = Lengkap + Lebih Baik + Tidak Ngelag

- [ ] `cargo test` **100%** — `workspace_tests` 8 + `fs_tests` 18 + `editor.test` 10 + `browser.test` 6 + `archive/share/git/package/security` 24 + regresi 117 = **~183** passed 0 failed
- [ ] `cargo clippy --all-targets -- -D warnings` **0**, `cargo fmt --check` **0**
- [ ] `npm test` **pass** (`windowStore.test` 3 + `DesktopIcons.test` 2 + `FileExplorer.test` 10 + `editor.test` 10 + `browser.test` 6)
- [ ] `npm run build` **gzip ≤750 KiB** (now 92.32 → target <150 after prism/browser) + `check_budget.sh` `binary ≤50 MiB` ` PERF-001/002` Passed
- [ ] **Perf DoD Tidak Ngelag:**
  - [ ] `FM` 10k file grid virtualized scroll 60fps <16ms (test `test_fm_virt_10k`)
  - [ ] `FM-004` search debounce 300ms + Worker, `q` `..` 400
  - [ ] `ED-002` highlight 5k line <100ms
  - [ ] `DESK` drag `RAF`, `Widget` poll 10s (not flood)
  - [ ] `BR` 20 tabs <50ms
- [ ] `tests/traceability.md` `WS-001..008` `DESK-001..008` `FM-001..018` `ED-001..010` `BR-001..006` `SEC-004..006` **Passed**
- [ ] `graphify update .` **948→~1050 nodes** `GRAPH_REPORT.md` cohesion naik, no `http.rs` 1300+ split (reuse `AllowedRoots` `src/files.rs`, `src/workspace.rs` separate)
- [ ] Manual E2E: Windows Explorer recreation (nav, breadcrumb, view, search, preview, context 9 verbs, history), VS Code tabs/syntax/find/terminal, Browser tabs/bookmarks — tanpa lag pada low-end 1 GiB Termux

