# Companion Checklist — v0.4 Plan: Developer Suite

- **Spec Reference**: [`004.plan-v0.4-suite.md`](./004.plan-v0.4-suite.md)
- **Status**: Ready for Review / Proposed
- **TDD Requirement**: Red → Green → Refactor for every milestone.

---

## 1. Milestone A — Git UI (`GIT-001..010`)

### Test Suite (`tests/git_tests.rs`)
- [ ] Red: `test_git_001_status_porcelain_parsing` (branch, ahead/behind, staged/unstaged/untracked).
- [ ] Red: `test_git_002_diff_unified_parser` (hunk structure, line mapping, add/remove).
- [ ] Red: `test_git_003_staging_and_unstaging_atomic` (stage file, unstage, stage-all).
- [ ] Red: `test_git_004_commit_creation_and_amend` (commit message, signature).
- [ ] Red: `test_git_005_branch_list_create_checkout` (branch switching).
- [ ] Red: `test_git_006_command_injection_rejection` (metacharacters in branch/commit sanitized).
- [ ] Red: `test_git_007_sandbox_containment` (repository path outside `AllowedRoots` rejected).

### Backend Implementation (`src/git.rs` & `src/http.rs`)
- [ ] Implement `GitService` with direct `std::process::Command` (no shell interpolation).
- [ ] Implement `GET /api/v1/git/status` endpoint handler.
- [ ] Implement `GET /api/v1/git/diff` endpoint handler.
- [ ] Implement `POST /api/v1/git/stage` endpoint handler.
- [ ] Implement `POST /api/v1/git/commit` endpoint handler.
- [ ] Implement `GET /api/v1/git/branches` & `POST /api/v1/git/branch/checkout`.
- [ ] Implement `POST /api/v1/git/remote/action` streaming execution.
- [ ] Green: All `tests/git_tests.rs` tests pass.

---

## 2. Milestone B — Package Center (`PKG-001..008`)

### Test Suite (`tests/package_tests.rs`)
- [ ] Red: `test_pkg_001_detect_system_backend` (Termux `pkg`, Debian `apt`, Arch `pacman`, Alpine `apk`).
- [ ] Red: `test_pkg_002_list_installed_packages` (parse name, version, status).
- [ ] Red: `test_pkg_003_search_repository_packages` (validate query string sanitization).
- [ ] Red: `test_pkg_004_package_name_regex_rejection` (reject command injection tokens).
- [ ] Red: `test_pkg_005_streaming_runner_task` (stream stdout/stderr over WebSocket task channel).

### Backend Implementation (`src/packages.rs` & `src/http.rs`)
- [ ] Implement `PackageManager` trait & platform backend resolvers.
- [ ] Implement `GET /api/v1/packages/backend` endpoint handler.
- [ ] Implement `GET /api/v1/packages/installed` endpoint handler.
- [ ] Implement `GET /api/v1/packages/upgradable` endpoint handler.
- [ ] Implement `GET /api/v1/packages/search` endpoint handler.
- [ ] Implement `POST /api/v1/packages/task` and `/api/v1/packages/stream/<task_id>` WebSocket handler.
- [ ] Green: All `tests/package_tests.rs` tests pass.

---

## 3. Milestone C — Read-only Share Links (`SHR-001..008`)

### Test Suite (`tests/share_tests.rs`)
- [ ] Red: `test_shr_001_create_share_token_csp_rng` (256-bit entropy, registered).
- [ ] Red: `test_shr_002_expired_token_rejected` (TTL expired returns 404/401).
- [ ] Red: `test_shr_003_terminal_share_drops_input` (WebSocket input frames ignored/dropped).
- [ ] Red: `test_shr_004_file_share_readonly` (GET allowed, PUT/POST/DELETE blocked 403).
- [ ] Red: `test_shr_005_instant_revocation` (DELETE token immediately terminates active connections).

### Backend Implementation (`src/share.rs` & `src/http.rs`)
- [ ] Implement `ShareRegistry` with TTL cleaner & max views counter.
- [ ] Implement `POST /api/v1/share/create` endpoint handler.
- [ ] Implement `DELETE /api/v1/share/<token>` endpoint handler.
- [ ] Implement read-only WebSocket handler `/api/v1/share/ws/terminal/<token>`.
- [ ] Implement read-only file/folder handler `/share/file/<token>` and `/share/folder/<token>`.
- [ ] Green: All `tests/share_tests.rs` tests pass.

---

## 4. Milestone D — Archive Manager (`ARC-001..008`)

### Test Suite (`tests/archive_tests.rs`)
- [ ] Red: `test_arc_001_inspect_zip_and_tar` (tree structure, uncompressed size tally).
- [ ] Red: `test_arc_002_extract_zip_clean` (clean file restoration).
- [ ] Red: `test_arc_003_extract_tar_gz_clean` (tar.gz restoration).
- [ ] Red: `test_arc_004_zip_slip_traversal_rejected` (`../../etc/passwd` stripped / rejected).
- [ ] Red: `test_arc_005_zip_bomb_byte_budget_enforced` (abort when extraction > budget).
- [ ] Red: `test_arc_006_archive_create_roundtrip` (zip/tar creation & 100% roundtrip extract).

### Backend Implementation (`src/archive.rs` & `src/http.rs`)
- [ ] Add `zip` and `tar` / `flate2` dependencies to `Cargo.toml`.
- [ ] Implement `ArchiveService::inspect` (header-only scanning).
- [ ] Implement `ArchiveService::extract` with streaming byte counter and path sanitization.
- [ ] Implement `ArchiveService::create` for zip and tar.gz.
- [ ] Implement endpoints `/api/v1/archive/inspect`, `/api/v1/archive/extract`, `/api/v1/archive/create`.
- [ ] Green: All `tests/archive_tests.rs` tests pass.

---

## 5. Milestone E — Desktop Frontend & UI (`WIN-010..013`)

### Frontend Components (`web/src/*`)
- [ ] Implement `web/src/apps/git/GitView.tsx` (Status, Diff, Staging, Commit, Branch).
- [ ] Implement `web/src/apps/packages/PackageCenterView.tsx` (Installed, Updates, Search, Terminal Log).
- [ ] Implement `web/src/apps/archive/ArchiveModal.tsx` (Inspect, Extract dialog).
- [ ] Implement `web/src/apps/share/ShareModal.tsx` (Create share link dialog).
- [ ] Register new apps in `web/src/desktop/appRegistry.ts` and `AppLauncher.tsx`.
- [ ] Add File Explorer context menu items (Git, Archive, Share).
- [ ] Run `npm test` & verify bundle size with `npm run build`.

---

## 6. Definition of Done & Quality Checks

- [ ] `cargo test` passes 100% (all unit & integration test suites).
- [ ] `cargo clippy --all-targets -- -D warnings` clean (0 warnings).
- [ ] `cargo fmt --check` clean.
- [ ] `npm test` passes in `web/`.
- [ ] `npm run build` succeeds under asset size budget (`PERF-002`).
- [ ] `tests/traceability.md` updated with all requirement statuses.
- [ ] `graphify update .` executed and verified.
