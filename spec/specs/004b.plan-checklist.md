# Companion Checklist — v0.4 Plan: Developer Suite

- **Spec Reference**: [`004.plan-v0.4-suite.md`](./004.plan-v0.4-suite.md) (audit 2026-08-21 incorporated)
- **Status**: Proposed — Hardening Gate PRE wajib hijau sebelum milestone fitur
- **TDD Requirement**: Red → Green → Refactor for every milestone (per `000a.plan-test.md:§2`)
- **Phasing**: Opsi A Iterative — PRE → v0.4.0 (Archive+Share) → v0.4.1 (Git) → v0.4.2 (Package). Opsi B monolit hanya fallback.

> **Gate Rule:** Tidak ada PR fitur v0.4 di-merge sebelum `§0` PRE lulus penuh (H1/H2/M1/M2). `cargo test` + `clippy -D warnings` + `fmt --check` + `graphify update .` adalah gate per milestone.

---

## 0. Milestone PRE — Hardening Gate (SEC-004..006) — WAJIB PERTAMA

> Sumber: audit 2026-08-21 `§1.2` H1/H2/M1/M2/M3. Estimasi 1 sprint.

### Test Suite Baru (`tests/security_tests.rs`)

- [ ] Red: `test_sec_004_security_headers_on_all_routes` — GET `/`, `/api/v1/health`, `/api/v1/fs/file?root=home&path=a.txt` mengandung `Content-Security-Policy: default-src 'self'`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, dan `Strict-Transport-Security` hanya saat `tls_enabled` (`M1` `src/http.rs:472` existing `nosniff` diperluas global).
- [ ] Red: `test_sec_005_csrf_token_required_for_post` — POST `/api/v1/fs/file` tanpa `X-CSRF-Token` (padahal cookie `__Host-csrf` ada) → `403 CSRF_REQUIRED`; GET tetap `200` (`M2` `src/http.rs:288-320` Origin-only → double-submit).
- [ ] Red: `test_sec_006_csprng_session_ids_not_predictable` — burst 100 `create_session` → semua `id`/`tok`/`att` unik, tidak sequential, tidak nanos-hex; validasi pakai `getrandom` path (`H1` `src/session.rs:367`).
- [ ] Red: `test_sec_006_real_ip_rate_limit` — 2 IP berbeda di-hammer 30+ kali → IP A ter-limit, IP B tidak (`H2` `src/http.rs:148` hardcode `127.0.0.1` → `ConnectInfo`).
- [ ] Red: `test_sec_006_constant_time_compare_subtle` — `pair()` dengan secret salah panjang berbeda tetap constant-time (ganti `src/auth.rs:215-225` ke `subtle`).

### Backend Implementation (`src/session.rs` & `src/http.rs` & `src/auth.rs` & `src/routes/*`)

- [ ] Ganti `rand_id()` `src/session.rs:367-374` (nanos hex) → `getrandom` 16-32 byte hex/base64url; audit `src/session.rs:165,166,279` semua callsite (`H1`).
- [ ] `pair_handler` `src/http.rs:147` ambil IP nyata via `ConnectInfo<SocketAddr>` (Axum 0.7 `into_make_service_with_connect_info`) dan teruskan ke `state.auth.pair(&secret, &real_ip)` (`H2`).
- [ ] Tambah middleware `security_headers` global di `create_router` `src/http.rs:61` — header di atas; `HSTS` hanya jika `state.config.tls_enabled()` (`M1`).
- [ ] Tambah middleware `csrf_guard` — double-submit cookie `__Host-csrf` (CSPRNG) + validasi `X-CSRF-Token` untuk `POST/PUT/DELETE/PATCH` non-GET/HEAD (`M2`). Issue cookie saat `GET /` atau `POST /pair` success.
- [ ] Ganti `constant_time_eq` `src/auth.rs:215-225` → `subtle::ConstantTimeEq` (tambah dep `subtle = "2.6"` di `Cargo.toml:15`) (`M3`).
- [ ] Refactor `src/http.rs:695` split → `src/routes/mod.rs`, `terminal.rs`, `fs.rs`, `auth.rs`, `health.rs`, `middleware.rs` (cohesion `0.11` `graphify-out/GRAPH_REPORT.md:87`).
- [ ] Green: All `tests/security_tests.rs` tests pass + existing `cargo test` regresi hijau (78+ tests `002.terminal-resize-completed.md:25` baseline).
- [ ] Docs: update `tests/traceability.md` `SEC-004..006` → `Planned` → `Passed` setelah hijau.

---

## 1. Milestone A — Git UI (`GIT-001..010`) — v0.4.1

### Test Suite (`tests/git_tests.rs`)

- [ ] Red: `test_git_001_status_porcelain_parsing` (branch, ahead/behind, staged/unstaged/untracked).
- [ ] Red: `test_git_002_diff_unified_parser` (hunk structure, line mapping, add/remove).
- [ ] Red: `test_git_003_staging_and_unstaging_atomic` (stage file, unstage, stage-all).
- [ ] Red: `test_git_004_commit_creation_and_amend` (commit message, signature).
- [ ] Red: `test_git_005_branch_list_create_checkout` (branch switching).
- [ ] Red: `test_git_006_command_injection_rejection` (metacharacters in branch/commit sanitized — tanpa `sh -c`, argv array `src/git.rs`).
- [ ] Red: `test_git_007_sandbox_containment` (repository path outside `AllowedRoots` `src/paths.rs:64` rejected `400`).
- [ ] Red: `test_git_008_csprng_tokens_for_git_pty` (remote PTY token pakai CSPRNG post-H1).

### Backend Implementation (`src/git.rs` & `src/http.rs` / `src/routes/git.rs`)

- [ ] Implement `GitService` with direct `std::process::Command` (no shell interpolation) + `git check-ref-format` validate.
- [ ] Implement `GET /api/v1/git/status` endpoint handler (AllowedRoots resolve + porcelain v2 parse).
- [ ] Implement `GET /api/v1/git/diff` endpoint handler (hunk parser).
- [ ] Implement `POST /api/v1/git/stage` endpoint handler (atomic `git add` / `restore --staged`).
- [ ] Implement `POST /api/v1/git/commit` endpoint handler (message sanitize, amend).
- [ ] Implement `GET /api/v1/git/branches` & `POST /api/v1/git/branch/checkout`.
- [ ] Implement `POST /api/v1/git/remote/action` streaming execution (delegate ke PTY jika butuh credential input, tanpa store credential).
- [ ] Green: All `tests/git_tests.rs` tests pass.
- [ ] Traceability: `GIT-001..008` → Passed.

---

## 2. Milestone B — Package Center (`PKG-001..008`) — v0.4.2 (Terakhir, Non-Sudo Dulu)

> **Scope lock:** v0.4.2 hanya read-only list/search + streaming task non-privileged. `sudo` interaktif via PTY **ditunda ke v0.5** (lihat `004.plan-v0.4-suite.md:§12.4`). Jangan auto-sudo.

### Test Suite (`tests/package_tests.rs`)

- [ ] Red: `test_pkg_001_detect_system_backend` (Termux `pkg`, Debian `apt`, Arch `pacman`, Alpine `apk`).
- [ ] Red: `test_pkg_002_list_installed_packages` (parse name, version, status).
- [ ] Red: `test_pkg_003_search_repository_packages` (validate query string sanitization — min 2 char, regex `PKG-005`).
- [ ] Red: `test_pkg_004_package_name_regex_rejection` (reject command injection tokens `curl; reboot`, `$(rm)`).
- [ ] Red: `test_pkg_005_streaming_runner_task` (stream stdout/stderr over WebSocket task channel, exit code reported).

### Backend Implementation (`src/packages.rs` & `src/http.rs` / `src/routes/packages.rs`)

- [ ] Implement `PackageManager` trait & platform backend resolvers (detect `pkg`/`apt`/`pacman`/`apk` via `which` + `/etc/os-release`).
- [ ] Implement `GET /api/v1/packages/backend` endpoint handler (capabilities: root_required, update_support).
- [ ] Implement `GET /api/v1/packages/installed` endpoint handler (argv tanpa shell, regex gate `^[a-zA-Z0-9][a-zA-Z0-9+._-]*$`).
- [ ] Implement `GET /api/v1/packages/upgradable` endpoint handler.
- [ ] Implement `GET /api/v1/packages/search` endpoint handler (sanitize `q`, limit length).
- [ ] Implement `POST /api/v1/packages/task` and `/api/v1/packages/stream/<task_id>` WebSocket handler (reuse `SessionRegistry` pattern, tanpa `sudo`).
- [ ] Green: All `tests/package_tests.rs` tests pass.
- [ ] Traceability: `PKG-001..006` → Passed (`PKG-007/008` tunda v0.5 jika butuh sudo).

---

## 3. Milestone C — Read-only Share Links (`SHR-001..008`) — v0.4.0 (Bersama Archive)

### Test Suite (`tests/share_tests.rs`)

- [ ] Red: `test_shr_001_create_share_token_csp_rng` (256-bit entropy, `getrandom` bukan nanos, registered di `ShareRegistry` — post-H1).
- [ ] Red: `test_shr_002_expired_token_rejected` (TTL expired returns 404/401 + constant-time compare `subtle`).
- [ ] Red: `test_shr_003_terminal_share_drops_input` (WebSocket Input/Resize frames ignored/dropped, hanya `Output`/`Exit`/`Pong` diteruskan).
- [ ] Red: `test_shr_004_file_share_readonly` (GET allowed, PUT/POST/DELETE blocked `403` via `ReadOnlyGuard`).
- [ ] Red: `test_shr_005_instant_revocation` (DELETE token immediately terminates active WS connections).
- [ ] Red: `test_shr_006_readonly_guard_non_get_blocked` (new hardening `SEC` cross-check).

### Backend Implementation (`src/share.rs` & `src/http.rs` / `src/routes/share.rs`)

- [ ] Implement `ShareRegistry` with TTL cleaner (tokio interval), max views counter, `getrandom` token (`SHR-001` pakai CSPRNG post-H1).
- [ ] Implement `POST /api/v1/share/create` endpoint handler (`ttl_seconds` + `max_views`).
- [ ] Implement `DELETE /api/v1/share/<token>` endpoint handler (revoke + disconnect).
- [ ] Implement read-only WebSocket handler `/api/v1/share/ws/terminal/<token>` (drop Input/Resize, log warn).
- [ ] Implement read-only file/folder handler `/share/file/<token>` and `/share/folder/<token>` (GET/HEAD only, via `AllowedRoots` + `ReadOnlyGuard`).
- [ ] Green: All `tests/share_tests.rs` tests pass.
- [ ] Traceability: `SHR-001..006` → Passed.

---

## 4. Milestone D — Archive Manager (`ARC-001..008`) — v0.4.0 (Bersama Share)

### Test Suite (`tests/archive_tests.rs`)

- [ ] Red: `test_arc_001_inspect_zip_and_tar` (tree structure, uncompressed size tally tanpa ekstrak ke disk).
- [ ] Red: `test_arc_002_extract_zip_clean` (clean file restoration, permission aman).
- [ ] Red: `test_arc_003_extract_tar_gz_clean` (tar.gz restoration).
- [ ] Red: `test_arc_004_zip_slip_traversal_rejected` (`../../etc/passwd`, absolut `/etc/passwd`, symlink escape — semua via `AllowedRoots`).
- [ ] Red: `test_arc_005_zip_bomb_byte_budget_enforced` (abort when extraction > `MUX_WEB_MAX_EXTRACT_SIZE` 500 MiB, streaming counter).
- [ ] Red: `test_arc_005b_zip_bomb_file_count_enforced` (abort > `MUX_WEB_MAX_EXTRACT_FILES` 10k).
- [ ] Red: `test_arc_005c_zip_bomb_expansion_ratio_enforced` (abort > 100:1).
- [ ] Red: `test_arc_006_archive_create_roundtrip` (zip/tar creation & 100% roundtrip extract checksum).

### Backend Implementation (`src/archive.rs` & `src/http.rs` / `src/routes/archive.rs`)

- [ ] Add `zip` and `tar` / `flate2` dependencies to `Cargo.toml:15` (pin semver, `cargo audit` clean).
- [ ] Implement `ArchiveService::inspect` (header-only scanning, tanpa tulis ke disk).
- [ ] Implement `ArchiveService::extract` with streaming byte counter + file count + expansion ratio + path sanitization via `AllowedRoots::resolve_path`.
- [ ] Implement `ArchiveService::create` for zip and tar.gz (sandbox-bound).
- [ ] Implement endpoints `/api/v1/archive/inspect`, `/api/v1/archive/extract`, `/api/v1/archive/create` (POST body `root` + `archive_path` + `destination_dir` resolved via `AllowedRoots`).
- [ ] Green: All `tests/archive_tests.rs` tests pass.
- [ ] Traceability: `ARC-001..007` → Passed.

---

## 5. Milestone E — Desktop Frontend & UI (`WIN-010..013`)

### Frontend Components (`web/src/*`)

- [ ] Implement `web/src/apps/git/GitView.tsx` (Status, Diff, Staging, Commit, Branch) — streaming diff dengan `encode_frame` `Frame::Output` reuse.
- [ ] Implement `web/src/apps/packages/PackageCenterView.tsx` (Installed, Updates, Search, Terminal Log) — task stream `/api/v1/packages/stream/<task_id>` WS.
- [ ] Implement `web/src/apps/archive/ArchiveModal.tsx` (Inspect, Extract dialog — budget indicator 500 MiB/10k).
- [ ] Implement `web/src/apps/share/ShareModal.tsx` (Create share link dialog — TTL + copy-to-clipboard, CSPRNG token display).
- [ ] Register new apps in `web/src/desktop/appRegistry.ts` and `AppLauncher.tsx` + `windowStore` (`WIN-010..013`).
- [ ] Add File Explorer context menu items (Git, Archive, Share) — right-click `root_id` aware.
- [ ] Run `npm test` & verify bundle size with `npm run build` (`PERF-002` 750 KiB gzip).
- [ ] A11y: `WIN-010..013` windows pass `A11Y-001..005` (keyboard nav, focus trap, ARIA, 44x44px, reduced-motion).

---

## 6. Definition of Done & Quality Checks (Per Fase + Final)

> Gate per fase: `cargo test` + `clippy -D warnings` + `fmt --check` + `npm test` + `npm run build` + `graphify update .`

- [ ] **PRE Gate**: `tests/security_tests.rs` 5/5 passed + `subtle` dep + `security_headers` + `csrf` + real IP + `rand_id` CSPRNG + `http.rs` split. `SEC-004..006` Passed.
- [ ] **v0.4.0 Gate**: `cargo test` 100% (`archive_tests` 8 + `share_tests` 6 + regresi 78+). Clippy 0 warn, fmt clean, traceability `ARC`+`SHR` Passed, `graphify update .` cohesion naik.
- [ ] **v0.4.1 Gate**: `git_tests` 8/8 passed, No-shell fuzz Passed, `GIT` Passed, graph update.
- [ ] **v0.4.2 Gate**: `package_tests` 5/5 passed, regex anti-injection fuzz Passed, `PKG` Passed, graph update.
- [ ] **Frontend Gate**: `npm test` passes, `npm run build` succeeds under budget (`PERF-002` 750 KiB gzip), `web` tests for `GitView`/`PackageCenter`/`ArchiveModal`/`ShareModal`.
- [ ] **Final DoD**: `cargo test` passes 100% (all suites), `cargo clippy --all-targets -- -D warnings` clean, `cargo fmt --check` clean, `npm test` + `npm run build` under budget, `tests/traceability.md` updated (`GIT`/`PKG`/`SHR`/`ARC`/`SEC` all Passed), `graphify update .` executed and verified per `AGENTS.md:19`, no `H`/`M1`/`M2` open.
