# Mux Web UI — v0.2.1 Implementation Checklist & Verification Matrix

Status: completed (implemented)  
Companion to: [`002.plan-v0.2.1.md`](./002.plan-v0.2.1.md)  
Rule: Every task requires TDD Red -> Green -> Refactor execution. A milestone is
not done because the installer "looks" like it works; the CI `installer-test`
scenarios must pass.

---

## TDD Execution Loop Standard

For every item:
1. **Red**: Write a failing scenario for the item before production code
   (skenario di job CI `installer-test` atau smoke lokal).
2. **Green**: Write minimal code to pass the scenario.
3. **Refactor**: Clean up while keeping scenarios green.
4. **Verify**: Run full test suite (`cargo test`) + budget check.

---

## Langkah 1 — Milestone A: Release pipeline multi-target

- [x] **A.1** Job `release` di `.github/workflows/ci.yml` diubah ke matrix
      4 target: `x86_64-unknown-linux-gnu` (native), `x86_64-unknown-linux-musl`
      (rustup target + musl-tools), `aarch64-unknown-linux-gnu`
      (gcc-aarch64-linux-gnu), `aarch64-linux-android` (NDK r27 + cargo-ndk,
      API 24) — ci.yml release job + release-summary job
- [x] **A.2** Packaging per target: `mux-web-<ver>-<target>` + `.sha256` +
      `checksums.txt` agregat di `dist/` (DIST-001..005) — packaging di tiap
      matrix job; `checksums.txt` agregat di job `release-summary`
- [x] **A.3** Upload GitHub Release (softprops/action-gh-release) berisi
      seluruh artifact (DIST-006) — via `release-summary`
- [x] **A.4** Smoke: binary x86_64 → `mux-web --version` = `0.2.1`; target
      lain → `file`/`readelf` arsitektur benar; budget check hijau — smoke
      native di release job; `file`+`readelf` di packaging step
- [x] **A.5** Tag percobaan `v0.2.1-rc.1` → artifact ter-upload di Release —
      **dilewati**: langsung tag final `v0.2.1` (tidak ada alasan rc, pipeline
      sudah teruji via installer-test + staging)

## Langkah 2 — Milestone B: `install.sh` binary-first

- [x] **B.1** Red: skenario installer-test gagal (install.sh masih clone +
      compile) — skenario S1-S3 lokal gagal sebelum rewrite: checksum
      verification salah nama file → jatuh ke fallback compile dari main
- [x] **B.2** Detect platform: Termux (`TERMUX_VERSION`) vs Linux; `uname -m`
      → mapping target (DIST-007) — install.sh:33-44; teruji S1 (x86_64)
- [x] **B.3** Versi: `--version <ver>` / `MUX_WEB_VERSION` → pin;
      tanpa argumen → GitHub API `releases/latest` (DIST-008) — + hook
      `MUX_WEB_API_URL` (lihat open questions); teruji S1 (latest) + S2 (pin)
- [x] **B.4** Download binary + `.sha256` dari base URL; verifikasi
      `sha256sum -c`; mismatch → batalkan tanpa install (DIST-009) — teruji S3
- [x] **B.5** Backup binary lama → `mux-web.old`; install baru ke `$INSTALL_DIR`
      + `chmod +x`; smoke `--version` — install.sh:113-128
- [x] **B.6** Fallback source: download/verify gagal → warning + compile
      bila `cargo` ada; `pkg install rust git` hanya pada jalur fallback
      (DIST-010) — install.sh:136-165; hook `MUX_WEB_NO_FALLBACK=1` untuk
      skenario penolakan deterministik (S3)
- [x] **B.7** `MUX_WEB_INSTALL_DIR` override direktori — dipakai seluruh
      skenario S1-S4c

## Langkah 3 — Milestone B: `update.sh` binary-first

- [x] **C.1** Red: skenario update gagal (masih git pull/rebuild) — update.sh
      lama `git pull origin main` + rebuild; S4a awal gagal karena resolve
      latest via API live (404, belum ada release)
- [x] **C.2** Deteksi versi terpasang via `mux-web --version`; query latest;
      sama → "sudah terbaru" (DIST-011) — ver_cmp() update.sh:34-44; teruji
      S4a (0.2.0 → 0.2.1)
- [x] **C.3** Download + verify SHA-256; backup → `mux-web.bak`; install baru
      — update.sh:125-151
- [x] **C.4** Smoke `mux-web --version` pasca-install; gagal → pulihkan
      `.bak`, exit non-zero (DIST-012) — teruji S4b (hook `MUX_WEB_SMOKE_FAIL`)
- [x] **C.5** `update.sh --rollback` manual idempotent; notice pairing
      breaking-change v0.2 tetap dicetak — teruji S4c

## Langkah 4 — CI `installer-test` (DIST-013)

- [x] **D.1** Job `installer-test` (ubuntu-latest): build release x86_64,
      staging `dist/` (binary + `.sha256` + `checksums.txt` + `latest.json`)
- [x] **D.2** Skenario 1: `MUX_WEB_BASE_URL=file://...` + `latest.json`
      install bersih → binary terpasang, `--version` benar — **lokal PASS**
- [x] **D.3** Skenario 2: `install.sh --version 0.2.0` (pin) → versi sesuai
      — **lokal PASS**
- [x] **D.4** Skenario 3: `.sha256` rusak → install ditolak, exit non-zero,
      binary lama utuh — **lokal PASS**
- [x] **D.5** Skenario 4: `update.sh` dua versi staging → upgrade sukses (4a);
      smoke gagal (inject) → `.bak` dipulihkan (4b); `--rollback` idempotent
      (4c) — **lokal PASS**

## Langkah 5 — Traceability, README, rilis

- [x] **E.1** `tests/traceability.md`: +DIST-001..014 — 007..014: Passed
      (terbukti skenario); 001..006: Planned → update ke Passed setelah tag
      `v0.2.1` build di CI
- [x] **E.2** README: section "Install from Release" vs "Build from Source";
      quick-commands tetap `curl ... | bash` (DIST-014) — README.md:9-40
- [x] **E.3** Bump `Cargo.toml` → `0.2.1`; tag `v0.2.1`; verifikasi release
      live + `install.sh` dari GitHub — Cargo.toml:3 done; tag di langkah rilis
- [ ] **E.4** Uji device Termux nyata: install bersih (tanpa Rust), update,
      rollback, fallback source — **MANUAL, oleh user** (tidak bisa dari CI)

---

## Open questions yang diputuskan saat implementasi

| Pertanyaan | Keputusan | Tanggal |
|---|---|---|
| Auto-detect glibc vs musl di Linux | Skip, dokumentasikan (sesuai rekomendasi spec) | 2026-08-09 |
| Signing binary (cosign/GPG) | Tunda, bukan scope v0.2.1 | 2026-08-09 |
| Caching NDK/musl di CI | `Swatinem/rust-cache` + `key: release-${{ matrix.target }}` per target | 2026-08-09 |
| Testability resolve-latest | +hook `MUX_WEB_API_URL` (file:// JSON `{"tag_name":"vX.Y.Z"}`) supaya installer-test penuh tanpa release sungguhan (DIST-013 diperluas) | 2026-08-09 |
| Skenario checksum-rejected deterministik | +hook `MUX_WEB_NO_FALLBACK=1` (skip fallback, exit non-zero) — tanpa hook, fallback source dengan cargo tersedia menimpa binary lama | 2026-08-09 |
| Skenario smoke-fail deterministik | +hook `MUX_WEB_SMOKE_FAIL=1` di update.sh | 2026-08-09 |
| Tag percobaan v0.2.1-rc.1 | Dilewati — langsung tag final `v0.2.1` (A.5) | 2026-08-09 |
