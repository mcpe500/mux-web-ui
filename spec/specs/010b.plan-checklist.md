# Companion Checklist — v0.6.1 Coding Agents & proot-distro, Anti-Timeout, Editor Engine

- **Spec**: [`010.plan-v0.6.1-coding-agents-proot-performance-editor.md`](./010.plan-v0.6.1-coding-agents-proot-performance-editor.md) (Implemented 2026-08-24)
- **Status**: Implemented — gates automatis hijau (cargo 161/0, vitest 70/70, budget PASS); manual smoke proot/agent = user-side
- **TDD**: Red → Green per milestone; gate `cargo test` + `clippy -D warnings` + `fmt --check` + `check_budget.sh` + `graphify update .`
- **Prinsip**: tanpa proot-distro terpasang perilaku identik v0.5.3 (regresi nol); sesi yang masih streaming output tidak boleh direap; budget aset ≤750 KiB tidak dilanggar

---

## 0. Ringkasan Deliverable

| Area | File | Perubahan |
|---|---|---|
| Config | `src/config.rs` | `session_busy_grace: u64` (+env), semantik `session_idle_timeout=0` |
| PTY | `src/pty.rs` | argv builder wrapper env (`proot-distro login …`) + exec agent |
| Session | `src/session.rs` | `last_output` timestamp; janitor busy-grace branch; skip rekap bila timeout=0 |
| HTTP | `src/http.rs` | `GET /environments`, `GET /environments/:id/agents`, payload `env_id`/`agent_id` |
| Frontend | `web/src/apps/terminal/TerminalView.tsx`, workspace UI | coalescing rAF; environment picker; Agents menu; label tab |
| Editor | `web/src/apps/editor/mux-code/*` | engine in-house tokenizer/model/viewport/search; lazy chunk |
| Tests | `tests/session_tests.rs` (baru `agent_tests.rs`), vitest | Red list di bawah |

---

## 1. Milestone A — proot-distro (`PROOT-001..006`)

### Red
- [x] Red unit: resolver distro dari fixture dir `installed-distros/{ubuntu,debian}` → 2 entry urut abjad; dir kosong → hanya `termux`; binary tak ada → hanya `termux`.
- [x] Red unit: builder argv — `env_id=ubuntu` → `["proot-distro","login","ubuntu","--",shell]`; tanpa `env_id` → `[shell]` (persis lama).
- [x] Red integration: `env_id:"nope"` → 400 `ENV_UNKNOWN`; argv wrapper terkunci unit `build_argv` (proot nyata = smoke user-side).
- [x] Red unit: `translate_cwd_for_env` — prefix shared dipertahankan, luar prefix → fallback HOME distro (spawn nyata = smoke user-side).

### Impl backend
- [x] `src/pty.rs` — `new_with_env` argv wrapper (fork-safe CString pre-fork) (resolve penuh SEBELUM fork, pola fork-safe existing `src/pty.rs:26-63`).
- [x] `src/environments.rs` — `list_distros` fs+CLI fallback, `list_environments`, `build_argv`, `translate_cwd_for_env` (3 unit tests).
- [x] `src/http.rs` — `env_id`/`agent_id` payload; `GET /api/v1/environments`; `GET /api/v1/environments/:id/agents`; 400 `ENV_UNKNOWN`/`AGENT_UNKNOWN`.
- [x] `SessionMetadata.env` (skip_serializing_if None — wire lama utuh).

### Frontend
- [x] Environment picker chips di header terminal panel.

### Green & gates
- [x] `cargo test` 100% • clippy/fmt 0.
- [x] Regresi TERM/LIFE/EDT hijau tanpa edit.

---

## 2. Milestone B — Coding Agents (`AGT-001..005`)

### Red
- [x] Red unit: registry statis 4 agent (`opencode`,`claude`,`codex`,`antigravity`) → argv quick-launch benar per {host,distro}.
- [x] Red unit: `probe_agent` + cache TTL 30s (`test_probe_cache`); host via PATH `which`; distro probe via proot (smoke user-side).
- [x] Guard MAX_SESSIONS: quick-launch jalur create sama → 409 terkunci `test_tab_001_*`.
- [x] Vitest/build: menu agents render (🤖), not-found disabled, label `[agent@env]` via labelOverride.

### Impl
- [x] Registry statis `src/agents.rs`; argumen eksekusi hanya dari registry.
- [x] Endpoint + cache `OnceLock<Mutex<ProbeCache>>` TTL 30s.
- [x] Quick-launch `build_agent_argv` → `sh -c "cd <cwd> && exec <binary>"` (shell-escape cwd).
- [x] UI Agents menu memakai cwd workspace aktif (FolderPicker reuse ditunda — catat handoff).

### Green & gates
- [x] `cargo test` 100% • clippy/fmt 0 • vitest hijau.
- [x] Q1: binary `agy` dipakai sesuai spec; runtime verify = smoke user-side (registry mudah diubah).

---

## 3. Milestone C — Anti-Timeout & Banter (`SESS-009/010`, `LIFE-010`, `PERF-006`) — PRIORITAS #1

### Red
- [x] Red integration `test_sess_009_busy_session_not_reaped`: sesi detached + output tiap <busy_grace → hidup ≥2× grace; hentikan output → tereap sesuai grace (perilaku lama terkunci).
- [x] Red integration `test_sess_010_idle_timeout_zero_disables_reap`: `--session-idle-timeout 0` → tidak pernah rekap idle; DELETE manual tetap jalan.
- [x] Heartbeat: server `Frame::Ping` tiap 20s; client pong 0x06 (Q3: frame protokol — D-log).
- [x] Coalescing rAF: pending buffer + 1 merged write/frame; cancelAnimationFrame di cleanup (flood smoke = user-side).

### Impl
- [x] `src/session.rs` — `last_output` di writer task; janitor busy-branch extend; detach/exit hormati grace=0.
- [x] `src/config.rs` — `--session-busy-grace`/env default 30; `session_idle_timeout=0` legal.
- [x] Heartbeat `Frame::Ping`/`Pong` (Q3 resolved); reconnect LIFE-004/007 existing.
- [x] Coalescing buffer mikro di `TerminalView.tsx` onmessage OUTPUT.

### Green & gates
- [x] Regresi penuh: SESS/LIFE hijau **tanpa edit test** (161/0).
- [ ] Manual smoke screen-off 10 mnt — user-side device.

---

## 4. Milestone D — Editor Engine "mux-code" (`EDIT-009..016`, `PERF-007`)

### Red (logic murni — vitest)
- [x] Tokenizer table-driven 7 bahasa + plain (`mux-code/tokenizer.ts`).
- [x] Model garis: insert/delete/multi-line + tests.
- [x] Search & replace: findMatches/replaceAll/gotoLine + tests.
- [x] `visibleRange` helper + tests (DOM virtualization penuh ditunda — highlight capped 200KB; handoff).

### Impl
- [x] Overlay: `<pre>` highlight di belakang textarea transparan, caret native, scroll sync.
- [x] Statusbar Ln:Col existing; wrap/font-size via style existing; bracket auto-close ditunda (handoff).
- [x] Lazy chunk tokenizer 1.2 KiB gzip terpisah; save/conflict jalur existing.

### Green & gates
- [x] Vitest 70/70; chunk `tokenizer-*.js` terpisah di build.
- [x] Budget PASS: total 113.53 KiB gzip (+1.65); tokenizer chunk 1.2 KiB.

---

## 5. Quality Gates & DoD

- [x] `cargo test` 100%: **161/0** — regresi nol.
- [x] clippy 0 · fmt 0.
- [x] `./scripts/check_budget.sh` PASS.
- [x] Versi → `0.6.1` (Cargo.toml + web/package.json).
- [x] `tests/traceability.md`: PROOT/AGT/SESS-009/010/LIFE-010/PERF-006/007/EDIT → Passed.
- [x] `graphify update .` sinkron; handoff 007 dibuat.
- [ ] Manual E2E user-side (Termux+proot): Ubuntu → quick-launch opencode → screen-off 10 mnt → sesi hidup → editor highlight.

## 6. Out of Scope (ingat)

Sandboxing dalam distro; LSP/minimapa/marketplace; server-in-proot; manajemen instalasi distro/agent; perubahan auth — spec §10.

## 7. Keputusan & Open Question Log

| # | Keputusan / Question | Status |
|---|---|---|
| D1 | Satu spec gabungan, milestone A-D | Decided (developer) |
| D2 | Busy-grace auto-extend + `idle-timeout 0` | Decided (developer) |
| D3 | Editor engine in-house ala Monaco, tanpa lib | Decided (developer) |
| D4 | List distro via filesystem `installed-distros/` | Decided (spec §4) |
| D5 | Argumen eksekusi hanya dari registry statis | Decided (spec §3) |
| Q1 | Nama binary Antigravity (`agy`?) | Open — verifikasi impl M-B |
| Q2 | Prefix shared cwd final (HOME/sdcard cukup?) | Open — konfirmasi developer |
| Q3 | Heartbeat wire: frame protokol vs WS control | Open — putuskan impl M-C |
