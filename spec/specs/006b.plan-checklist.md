# Companion Checklist — v0.6 Editor Integrated Terminal + Open File/Folder ala VS Code

- **Spec**: [`006.plan-v0.6-editor-terminal-openfolder.md`](./006.plan-v0.6-editor-terminal-openfolder.md) (approved 2026-08-21)
- **Status**: approved — siap Build
- **TDD**: Red → Green per item; gate `cargo test` + `vitest` + `clippy -D warnings` + `fmt --check` + `npm run build ≤750 KiB` + `graphify update .`
- **Keputusan**: cwd additive fields; 1 terminal/editor; persist per window id (lihat spec §9)

---

## 0. Ringkasan Deliverable

| Area | File | Perubahan |
|---|---|---|
| Backend | `src/http.rs:34,558` | `CreateTerminalReq +cwd_root/cwd_path`, resolve via `AllowedRoots` |
| Frontend | `web/src/apps/editor/TextEditorView.tsx` | Spawn/toggle terminal, split panel, menu File, shortcut, status bar |
| Frontend baru | `web/src/apps/files/FolderPicker.tsx` | Modal picker mode folder/file |
| Tests | `tests/session_tests.rs`, `web/src/apps/editor/editor.test.tsx`, `FolderPicker.test.tsx` | Red list di bawah |
| Docs | `tests/traceability.md` | `EDT-001..010` Planned→Passed |

---

## 1. Milestone A — Integrated Terminal (`EDT-001..005`)

### Red (backend)
- [ ] Red: `test_edt_002_cwd_ok` — `POST /terminals {"cols":80,"rows":24,"cwd_root":"home","cwd_path":"/sub"}` → `201`; tulis `pwd\n` via WS → output mengandung `/…/sub`.
- [ ] Red: `test_edt_002_cwd_traversal_400` — `{"cwd_path":"../.."}` → `400` body `PATH_TRAVERSAL`; **tidak** ada session bertambah.
- [ ] Red: `test_edt_002_cwd_nul_400` — `"a\0b"` → `400`.
- [ ] Red: regresi — request tanpa `cwd_*` tetap `201` dan berperilaku lama (`work_dir` config).

### Red (frontend vitest)
- [ ] Red: `test_edt_001_spawn_toggle_no_duplicate` — klik toggle 5× → `fetch('/api/v1/terminals', POST)` dipanggil **tepat 1×**; panel class hidden toggles.
- [ ] Red: `test_edt_003_split_resize_clamp` — drag divider ke ekstrem → `splitRatio` clamp `0.25..0.75`; tidak ada error.
- [ ] Red: `test_edt_004_restart_after_exit` — emit exit → overlay muncul; klik Restart → POST sekali lagi dengan cwd sama.
- [ ] Red: `test_edt_005_max_sessions_msg` — mock POST 409 → tampil pesan `MAX_SESSIONS`, tanpa crash.

### Impl backend (`src/http.rs`)
- [ ] Tambah field serde optional pada `CreateTerminalReq` (`src/http.rs:34`).
- [ ] Handler `create_terminal_handler` (`src/http.rs:558`) resolve cwd:
      `(Some(r),Some(p)) => Some(resolve_path(r,p)?)`; map err → `(400, PATH_TRAVERSAL)`.
- [ ] Pastikan `create_session(... work_dir ...)` memakai hasil resolve.

### Impl frontend (`TextEditorView.tsx`)
- [ ] State `termId/showTerm/splitRatio/spawnErr` + `toggleTerminal()` sesuai spec §A.3.
- [ ] Split panel flex-column + divider 6px drag (`PointerEvents`+`RAF`, clamp).
- [ ] Header terminal label `${ws.rootId}:${ws.basePath||'/'}`; tombol `↻ Restart`, `✕ Hide`.
- [ ] Render `<TerminalView terminalId={termId}/>` hanya saat `termId` ada.

### Green
- [ ] `cargo test --test session_tests` semua hijau termasuk Red baru (backend 4/4).
- [ ] `npm --prefix web test -- editor.test.tsx` hijau (4/4).

---

## 2. Milestone B — Open File & Open Folder (`EDT-006..010`)

### Red (vitest)
- [ ] Red: `test_edt_006_picker_mode_folder` — render `mode:'folder'`: file disabled; pilih folder → `onSelect({rootId:'home',path:'/work'})`.
- [ ] Red: `test_edt_006_picker_mode_file` — `mode:'file'`: double-click file → onSelect path file; breadcrumb click navigasi (mock fetch `/fs/entries`).
- [ ] Red: `test_edt_006_picker_lazy_depth1` — buka picker → hanya 1 call entries untuk root; masuk subfolder → call berikutnya path tersebut.
- [ ] Red: `test_edt_007_ws_persist_fallback` — set ws lalu remount → terbaca dari `localStorage["mux_editor_ws_<id>"]`; localStorage korup → fallback `{home,''}` tanpa crash.
- [ ] Red: `test_edt_007_tree_reload_on_folder_open` — Open Folder → `loadDir(root, base||'/')` dipanggil & expandedDirs reset.
- [ ] Red: `test_edt_008_shortcuts` — `Ctrl+O` buka picker file; `Ctrl+K` lalu `O` buka picker folder; `Ctrl+`` toggle terminal.
- [ ] Red: `test_edt_009_terminal_follows_folder` — setelah Replace → POST berikutnya membawa `cwd_path=base` baru.
- [ ] Red: `test_edt_010_statusbar_branch` — mock git status `# branch.head main` → status bar menampilkan `⎇ main`; 404 → segmen hilang.

### Impl
- [ ] `web/src/apps/files/FolderPicker.tsx` — props `mode/initial/onSelect/onClose`; roots sidebar; breadcrumb; listing depth-1; footer path input; Fluent styling konsisten (blur, radius 12px, border rgba .12).
- [ ] `TextEditorView.tsx` — state `ws` + persist; menu **File** tambah `Open File… Ctrl+O`, `Open Folder… Ctrl+K O`; chord handler `Ctrl+K` buffer 1.5s; status bar kiri folder aktif + kanan branch poll 30s (AbortController saat unmount/minimize).
- [ ] Integrasi EDT-009: sebelum spawn/restart terminal gunakan `ws` aktif; dialog `Replace/Keep` saat ganti folder dengan terminal hidup.

### Green
- [ ] `vitest FolderPicker.test.tsx` 3/3, `editor.test.tsx` total ≥10/10.
- [ ] Manual E2E checklist spec §B.6 semua ✅ (open folder → pwd benar, branch tampil).

---

## 3. Quality Gates & DoD

- [ ] `cargo test` **100%** (regresi 117 + EDT backend baru) — 0 failed.
- [ ] `cargo clippy --all-targets -- -D warnings` 0 • `cargo fmt --check` 0.
- [ ] `npm --prefix web test` pass • `npm run build` gzip **≤750 KiB** (tanpa dep baru).
- [ ] Perf: drag split stabil 60fps; picker navigasi instan; tidak ada PTY duplikat saat toggle (bukti test EDT-001).
- [ ] Security: traversal/NUL 400; guard MAX_SESSIONS ramah; localStorage invalid → fallback aman.
- [ ] `tests/traceability.md`: tambah `EDT-001..010` → **Passed**.
- [ ] `graphify update .` sinkron; `spec/specs/006*` lengkap; handoff singkat ditambahkan bila perlu.

---

## 4. Out of Scope (ingat)
Multi-terminal grid/tab, split editor multi-group, SSH remote, task runner/debugger — tunda v0.7 (spec §8).
