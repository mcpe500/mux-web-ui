# Companion Checklist — v0.6.2 Editor Polish & Virtualisasi, Agents UX, Persist Layout, Network Hardening, Signing, Distro Mgmt

- **Spec**: [`011.plan-v0.6.2-editor-agents-network-signing-distro.md`](./011.plan-v0.6.2-editor-agents-network-signing-distro.md)
- **Status**: Planned (2026-08-25)
- **TDD**: Red → Green per milestone; gate `cargo test` + `clippy -D warnings` + `fmt --check` + `vitest` + `check_budget.sh` + `graphify update .`
- **Prinsip**: regresi nol (161 cargo + 70 vitest hijau tanpa edit); tanpa flag baru perilaku identik v0.6.1; budget ≤750 KiB gzip; argv-only tanpa `sh -c`

---

## 0. Ringkasan Deliverable

| Area | File | Perubahan |
|---|---|---|
| Tokenizer | `web/src/apps/editor/mux-code/tokenizer.ts` | stateful block-comment (`tokenizeLineStateful`) |
| Brackets | `web/src/apps/editor/mux-code/brackets.ts` (baru) | auto-close/skip-over/wrap-seleksi murni |
| Virtualisasi | `web/src/apps/editor/mux-code/lineStates.ts` (baru), `TextEditorView.tsx` | cache state inkremental + windowed gutter/highlight, cap 2 MB |
| Wrap | `editorLogic.ts`, `TextEditorView.tsx` | toggle + persist + sinkron layer |
| Agents UX | `TextEditorView.tsx`, `editorLogic.ts` | FolderPicker cwd override + chip |
| Persist layout | `terminalTabsLogic.ts`, `TextEditorView.tsx` | serialize/restore aman |
| Network | `src/config.rs`, `src/http.rs`, `tests/security_tests.rs` | `--trusted-proxies`, wildcard allowlist |
| Signing | `.github/workflows/ci.yml`, `install.sh`, `update.sh`, `share/minisign.pub` | sign/verify checksums.txt |
| Distro mgmt | `src/distro_mgmt.rs` (baru), `src/http.rs`, `src/agents.rs`, UI modal | install/remove task stream + hints |
| Assurance | `ci.yml`, `tests/traceability.md` | cargo audit job + pemetaan SOC 2 TSC |

---

## 1. Milestone A — mux-code Polish (`EDIT-017`, `EDIT-013`, `EDIT-014`)

### Red
- [x] Vitest tokenizer: js/rust blok ≥3 baris; blok tak tertutup → sisa baris `com`; `//` di dalam blok tetap `com`; `/*` dalam string tidak membuka blok; py/sh/json/md tak berubah.
- [x] Vitest brackets tabel kasus: open→pair+caret-dalam; close identik→skip-over; seleksi→wrap; kutip antar word→tidak auto-close.
- [x] Vitest wrapStyles/persist: on↔off style benar; storage key per winId.

### Impl
- [x] `tokenizeLineStateful(line, lang, inBlock)` → `{tokens,inBlock}`; wrapper lama `tokenizeLine(line,lang): Token[]` kompatibel (70 test lama tanpa edit).
- [x] `mux-code/brackets.ts` murni + wire `handleKeyDown` (restore-caret pola Tab).
- [x] Wrap toggle: toolbar + menu View + chip statusbar; textarea & `<pre>` style sama; gutter disembunyikan saat ON.

### Green & gates
- [x] Vitest 100% · budget PASS.

---

## 2. Milestone B — Virtualisasi Penuh (`EDIT-018`)

### Red
- [x] Vitest lineStates: sync diff prefix/suffix; edit tengah hanya rekomputasi ekor; konsistensi tokensFor(window) == full pass; blok lintas window benar.

### Impl
- [x] `mux-code/lineStates.ts`: createCache/syncLines/update/tokensFor (+LINE_H=21).
- [x] `TextEditorView.tsx`: scrollTop rAF-throttled state; windowed gutter (spaser offset); windowed highlight `<pre>` translateY(+translateX); cap 200 KB → 2 MB.

### Green & gates
- [ ] Vitest 100% · budget PASS · smoke user-side file 10k baris. *(gates lokal lulus; smoke manual menunggu perangkat)*

---

## 3. Milestone C — FolderPicker cwd Agents (`AGT-006`)

### Red
- [x] Vitest `mergeSpawnPayload`: null picker → workspace aktif (perilaku lama); picker → cwd_root/cwd_path picker; env_id hanya non-termux; agent_id menyatu.

### Impl
- [x] Tombol `📁 cwd` → FolderPicker mode folder; state agentCwd + chip `[cwd: root:path] ✕`; spawnInto wsOverride dari agentCwd.

### Green & gates
- [x] Vitest 100% · tanpa endpoint baru.

---

## 4. Milestone D — Persist Layout Terminal (`TAB-010/011`)

### Red
- [x] Vitest serialize/restore: 2 hidup + 1 mati → 2 tab urut sama flex normal; semua mati → panel tertutup; >max clamp; dangling activeKey sembuh; JSON korup → kosong; key diregenerasi.

### Impl
- [x] `terminalTabsLogic.ts` serializeLayout/restoreLayout (pola safeLoadWorkspace).
- [x] Wire TextEditorView: persist debounce groups/splitRatio/panelOpen; on-mount GET /terminals → restore hanya sesi hidup.

### Green & gates
- [x] Vitest 100%.

---

## 5. Milestone E — Trusted Proxy + Wildcard Allowlist (`NET-007/008`)

### Red
- [x] Unit/integration trusted-proxies: parser CSV ip/cidr fail-fast entry bernomor; effective_client_ip matrix (peer untrusted+XFF → abaikan; chain trusted → klien asli; partial cidr tidak match); tanpa flag = perilaku lama persis.
- [x] Unit wildcard: valid `*.ts.net`; tolak `*`, `*.`, skema/port; dot-boundary match suffix sendiri + multi-level; warning startup mencantumkan entri wildcard.

### Impl
- [x] `src/config.rs`: field `trusted_proxies` + parser cidr std-only; `host_allowed(name,list)` wildcard matcher; valid_host_token menerima `*.suffix`.
- [x] `src/http.rs`: `effective_client_ip(peer, headers, cfg)` dipakai rate-limit pairing + tracing log; Host gate exact path tak berubah selain matcher.
- [x] Warning startup wildcard di `main.rs`.

### Green & gates
- [x] `cargo test` 100% regresi nol · clippy/fmt 0.

---

## 6. Milestone F — Release Signing (`SIGN-001..003`)

### Red / Scenario
- [x] Installer-test 5a sig valid lolos; 5b checksums dimodifikasi + sig lama → abort binari lama utuh; 5c strict-mode tanpa minisign → abort.

### Impl
- [x] CI release-summary: sign `checksums.txt` kondisional secret `MINISIGN_SIGNING_KEY` → upload `.minisig`; `share/minisign.pub` di-commit; prosedur generate offline terdokumentasi.
- [x] `install.sh`/`update.sh`: unduh checksums+minisig; minisign ada → verify SEBELUM sha256 (fail-closed tampered); tidak ada → SHA-256-only + warning kuning; `MUX_WEB_STRICT_VERIFY=1` fatal; pubkey via `MUX_WEB_MINISIGN_PUB` atau embedded.

### Green & gates
- [ ] Scenario CI hijau (fixture keypair sementara).

---

## 7. Milestone G — Manajemen Distro + Agent Hints (`DISTRO-001..004`)

### Red
- [x] Unit distro_mgmt: available_vs_installed (base dir injectable); install/remove argv persis; id liar → Err (regex + membership).
- [x] Integration (fake proot-distro PATH): POST install/remove → task_id; guard satu task → 409 TASK_BUSY; WS stream relay baris + exit code; DELETE cancel SIGTERM→SIGKILL; LIFE-002 no-orphan tetap hijau.
- [x] Vitest modal: render installed/available; disable saat busy; remove konfirmasi ketik-id.

### Impl
- [x] `src/distro_mgmt.rs` + routes `POST /environments/:id/install|remove`, `GET /environments/tasks/:id/stream`, `DELETE /environments/tasks/:id`.
- [x] `src/agents.rs` field `install_hint` statis (additive serialize).
- [x] UI modal ⚙️ environment chips.

### Green & gates
- [x] `cargo test` 100% · clippy/fmt 0 · vitest 100%.

---

## 8. Milestone H — Security Assurance (`SEC-007/008`)

- [x] Job `cargo audit` di ci.yml (gagal pada vulnerability; cron mingguan + workflow_dispatch).
- [x] Tabel kontrol→SOC 2 TSC ditambahkan ke `tests/traceability.md`.
- [x] Catatan SOC = audit organisasi di README (bagian Security).

---

## 9. Quality Gates & DoD

- [x] `cargo test` 100% regresi nol · clippy 0 · fmt 0.
- [x] Vitest 100% (lama tanpa edit) · `./scripts/check_budget.sh` PASS.
- [x] Versi → `0.6.2` (Cargo.toml + web/package.json).
- [x] `tests/traceability.md`: EDIT-013/014/017/018, AGT-006, TAB-010/011, NET-007/008, SIGN-001..003, DISTRO-001..004, SEC-007/008 → Passed.
- [x] `graphify update .` sinkron; handoff 008 dibuat.
- [ ] Manual E2E user-side (Termux+proot): quick-launch opencode via cwd picker; **verifikasi binary `agy` (Q1)**; screen-off 10 mnt sesi hidup; file 10k baris highlight+scroll; install/remove distro roundtrip; spoof XFF matrix; update.sh verifikasi signature.

## 10. Out of Scope (ingat)

LSP/minimapa/marketplace; sandboxing dalam distro; server-in-proot; ACME/TLS otomatis; RBAC; sudo interaktif Package Center; instalasi agent oleh server; sertifikasi SOC formal.

## 11. Keputusan & Open Question Log

| # | Keputusan / Question | Status |
|---|---|---|
| D1 | Satu spec gabungan A–H; semua kandidat masuk | Decided (user) |
| D2 | Security = kontrol engineering SOC 2 TSC + bukti | Decided (developer) |
| D3 | Virtualisasi overlay saja; textarea native utuh | Decided (§5 spec) |
| D4 | Instalasi agent server-side ditolak; hint statis | Decided (§3) |
| D5 | minisign, bukan cosign | Decided (§9) |
| D6 | Penamaan versi v0.6.2 walau setara minor | Decided (user) |
| Q1 | Binary Antigravity `agy`? | Open — smoke user-side |
| Q2 | Flag non-interaktif `proot-distro remove` | Open — putuskan impl M-G |
| Q3 | Wildcard single vs multi-level | Open — default multi-level |
