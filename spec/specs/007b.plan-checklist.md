# Companion Checklist — v0.5.1 Terminal CWD & Prompt Hotfix

- **Spec**: [`007.plan-v0.5.1-terminal-cwd-hotfix.md`](./007.plan-v0.5.1-terminal-cwd-hotfix.md) (Implemented 2026-08-22)
- **Status**: Implemented — semua gate hijau; bukti test tercantum per item
- **TDD**: Red → Green per milestone; gate `cargo test` + `vitest` + `clippy -D warnings` + `fmt --check` + `npm run build ≤750 KiB` + `graphify update .`
- **Keputusan**: reuse `GET /api/v1/health` (src/http.rs:50,76,142) untuk version gate; `work_dir` additive di create response; font Nerd subset + unicode11 (lihat spec §8)

> Setelah spec ini 100% Implemented, **install baru langsung v0.5.1** — verifikasi via
> `GET /api/v1/health → version:"0.5.1"` dan alur `Open Folder → pwd → git status` tanpa langkah manual.

---

## 0. Ringkasan Deliverable

| Area | File | Perubahan |
|---|---|---|
| Backend | `src/session.rs:22`, `src/http.rs:565-592` | Metadata `work_dir` di create response |
| Backend | `Cargo.toml:3` | `version = "0.5.1"` |
| Frontend | `web/src/apps/editor/TextEditorView.tsx:720` | Header pakai `meta.work_dir` + fallback |
| Frontend | `web/src/apps/editor/editorLogic.ts` | Helper `displayWorkDir`/`isVersionAtLeast` |
| Frontend | `web/src/apps/terminal/TerminalView.tsx:2-4,24` | `Unicode11Addon` + `fontFamily` Nerd chain |
| Frontend | `web/src/styles/main.css` + `web/public/fonts/*.woff2` | `@font-face` JetBrainsMono Nerd Font Mono subset |
| Frontend | `web/package.json:4,13-14` | `version "0.5.1"` + `@xterm/addon-unicode11` |
| Tests | `tests/session_tests.rs`, `tests/boot_tests.rs`, `web/src/apps/**/ *.test.ts*` | Red list di bawah |
| Docs | `tests/traceability.md` | `V051-001..006` Planned→Passed |

---

## 1. Milestone A — Backend: `work_dir` di Respons + Bump `0.5.1` (`V051-001`, `V051-005`)

### Red (backend — wajib gagal dulu)

- [x] Red: `test_v051_001_work_dir_in_create_resp` — `POST /api/v1/terminals {"cols":80,"rows":24,"cwd_root":"home","cwd_path":"/sub"}` → `201`; `body.work_dir` ada dan berakhiran `/sub` (atau absolute yang di-`canonicalize`).
- [x] Red: `test_v051_001_work_dir_empty_is_config` — `POST` tanpa `cwd_*` dan `{"cwd_path":""}` → `201` dan `body.work_dir` == `config.work_dir` (kompatibel lama).
- [x] Red (nama aktual `test_v051_001_traversal_no_work_dir` + regresi `test_edt_002_cwd_traversal_400`): — `{"cwd_path":"../.."}` → `400` body `PATH_TRAVERSAL`; **tidak** ada session bertambah (reuse `test_edt_002_cwd_traversal_400`).
- [x] Red: `test_v051_005_health_version_is_0_5_1` — `GET /api/v1/health` → `{"status":"ok","version":"0.5.1"}` (sebelum bump versi test harus gagal di `0.3.0`).

### Impl backend (`src/session.rs`, `src/http.rs`, `Cargo.toml`)

- [x] `src/session.rs:22-35` — tambah field `pub work_dir: Option<PathBuf>` pada struct yang di-serialisasi sebagai create response (atau struct wrapper baru `CreateTerminalResp`), `#[serde(skip_serializing_if = "Option::is_none")]`.
- [x] `work_dir` diisi di `SessionRegistry::create_session` (`src/session.rs:175-183`, clone sebelum `PtySession::new`) — handler tidak berubah karena resolve sudah menghasilkan nilai yang sama; serialisasi PathBuf → string absolute otomatis oleh serde.
- [x] `Cargo.toml:3` — `version = "0.5.1"` (otomatis `health.version` ikut).
- [x] `web/package.json:4` — `version = "0.5.1"` (sinkron dengan Rust; chip UI baca health, bukan package.json langsung).

### Green

- [x] `cargo test --test session_tests test_v051*` 4/4 hijau.
- [x] `cargo test --test boot_tests` 2/2 hijau.
- [x] `cargo test` **100%**: 125 passed / 0 failed (regresi EDT tetap hijau).
- [x] `cargo clippy --all-targets -- -D warnings` 0 • `cargo fmt --check` OK.

---

## 2. Milestone B — Frontend: Header = Server `work_dir` + Version Gate (`V051-002`, `V051-003`)

### Red (vitest)

- [x] Red (pure-logic `displayWorkDir`, node-env): — mock `POST /terminals` → `{id:"t1", work_dir:"/data/data/com.termux/files/home/printing-web-app"}` → header menampilkan basename/relative tersebut, `title` = absolute (bukan tebakan `ws`).
- [x] Red (pure-logic fallback + flag `legacy`): — mock respons **tanpa** `work_dir` (server lama) → header fallback ke `ws` (`home:/printing-web-app`) + banner kuning `Server lama` muncul (`data-testid="cwd-legacy-banner"`).
- [x] Red (gateState stale → banner red): — mock `GET /health → 0.3.0` → banner merah `butuh ≥0.5.1` (`data-testid="version-gate-banner"`); mock `0.5.1` → tidak ada banner.
- [x] Red (gateState chip ok/bad/warn): — chip status bar `data-testid="version-chip"` merah di `0.3.0`, hijau di `0.5.1`; fetch gagal → chip warning (tidak crash).
- [x] No-pwd-injection by design: jalur spawn (`spawnTerminal`) hanya POST create — tidak ada kode menulis ke WS; diverifikasi grep `web/src/apps/editor` (satu-satunya `pwd` di test backend EDT/V051). Verifikasi runtime menyusul di E2E Termux.

### Impl

- [x] `web/src/apps/editor/editorLogic.ts` — helper `displayWorkDir(workDir: string|undefined, ws: EditorWS): string` + `isVersionAtLeast(v:string, min:string): boolean` (semver sederhana `split('.')`).
- [x] `TextEditorView.tsx` header terminal — label via `wdLabel`: bila `meta.work_dir` ada → pakai itu; else fallback + set flag `legacyServer=true` → banner kuning.
- [x] `TextEditorView.tsx` — `useEffect` mount: `fetch /api/v1/health` → state `serverVersion`; banner merah + chip warna sesuai `isVersionAtLeast`.
- [x] Banner dismissible (`×`), `role="alert"`, tidak memblokir input.

### Green

- [x] vitest editorLogic: V051 total 15 deskripsi hijau (34 → 35 file-level pass saat Fase B).
- [x] Mock-based verifikasi lewat unit logic (header/banners/chip diputuskan pure fn `gateState`/`displayWorkDir`).

---

## 3. Milestone C — Prompt Rendering (`V051-004`)

### Red (vitest / build)

- [x] Red: `terminalConfig.test.ts` — stub semantik xterm (`loadAddon→activate→unicode.register`) memastikan provider v11 terdaftar + `activeVersion === '11'` (assertion behavior-based; nama kelas dimangle transform).
- [x] Red: `TERMINAL_FONT_STACK` dimulai dengan — `term.options.fontFamily` mengandung `JetBrainsMono Nerd Font` (atau cek `@font-face` ada di `document.styleSheets`).
- [x] Build budget: JS gzip **108.17 KiB** + font subset 42 KiB — jauh di bawah 750.

### Impl

- [x] `@xterm/addon-unicode11` ^0.8.0 terpasang (package.json + lockfile).
- [x] Wiring via `terminalConfig.ts`: `import { TERMINAL_FONT_STACK, applyTerminalAddons }`; from '@xterm/addon-unicode11'`; `term.loadAddon(new Unicode11Addon())`; `term.unicode.activeVersion = '11'` (setelah `open`).
- [x] `TerminalView.tsx` — `fontFamily: TERMINAL_FONT_STACK` "'JetBrainsMono Nerd Font Mono','JetBrains Mono','Fira Code',monospace"`.
- [x] `web/src/styles/main.css` — `@font-face { font-family: 'JetBrainsMono Nerd Font Mono'; src: url('/fonts/JetBrainsMonoNerdFontMono-Regular.woff2') format('woff2'); font-display: swap; unicode-range: U+E000-EBFF, U+2500-259F; }`
- [x] `web/public/fonts/JetBrainsMonoNF-Mono-subset.woff2` — **42,432 bytes**, sha256 `cbe7f107b987fd13e811a241945f7709ef1ba0d72721ec7978527560a8899df1`; source ttf sha256 `276cf05f…` (nerd-fonts v3.2.1).
- [x] `scripts/check_budget.sh` PASS (binary 2.74 MiB, frontend 108.17 KiB ≤750).

### Green

- [x] vitest `terminalConfig.test.ts` 3/3 hijau; visual E2E glyph menyusul di Termux (item §4 bawah).
- [x] vitest **38/38** • build gzip **108.16 KiB** (JS) — tercatat di handoff 004.

---

## 4. Quality Gates & DoD (`V051-006`)

- [x] `cargo test` **100%**: **125 passed / 0 failed**.
- [x] `cargo clippy --all-targets -- -D warnings` 0 • `cargo fmt --check` OK.
- [x] `npm --prefix web test` pass (38/38).
- [x] `npm run build` gzip **108.17 KiB** + font 42 KiB ≤ 750 KiB.
- [x] `tests/traceability.md`: `V051-001..006` → **Passed** (EDT tetap Passed).
- [x] `graphify update .` sinkron; `spec/specs/007*` lengkap + status Implemented.
- [ ] **Manual E2E di Termux nyata** (bukan mock) — checklist DoD spec §11.2 satu per satu:
  - [ ] `Open Folder [HOME]/printing-web-app` → tree reload.
  - [ ] `Ctrl+`` → `pwd` = `/…/home/printing-web-app` (bukan `$HOME`).
  - [ ] `git status` tanpa `fatal` (branch tampil di status bar `⎇ main`).
  - [ ] Header terminal konsisten dengan `pwd` (hover `title` = absolute).
  - [ ] Prompt powerline tanpa kotak rusak.
- [ ] **Jaminan install**: fresh `install.sh` pada Termux bersih → `GET /health → 0.5.1` + E2E di atas langsung benar (tulis bukti di handoff).

---

## 5. Out of Scope (ingat — jangan dikerjakan)

Multi-terminal grid/tab, split editor multi-group, SSH remote, task runner/debugger, perubahan `AllowedRoots`, OneDrive/Network — tunda (spec §9).

---

## 6. Keputusan & Open Question Log

| # | Keputusan / Question | Status | Catatan |
|---|---|---|---|
| Q1 | Reverse-map `work_dir` absolute → `rootId:relative` presisi vs basename+title | **MVP: basename + title** | Presisi bila mudah; jangan block Green |
| D1 | Reuse `health` untuk version gate | **Decided** | Tidak ada route baru |
| D2 | `work_dir` di JSON response, bukan header/WS | **Decided** | Additive, `skip_serializing_if None` |
| D3 | Penomoran `007` berversi `v0.5.1` (patch sebelum `0.6`) | **Assumption — minta konfirmasi** | Jika ingin `v0.6.1`, ubah `Cargo.toml` target sebelum Green |

---

## 7. Estimasi

Backend ±15 baris; `TextEditorView` ±40 baris; `TerminalView` ±10 baris + 1 font asset; tests ±120 baris; `check_budget` tetap hijau.
