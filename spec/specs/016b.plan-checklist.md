# 016b — Checklist Codex Studio (v0.6.7)

> Companion spec/specs/016.plan-v0.6.7-codex-studio.md — centang per item TDD.

## Milestone A — Session Engine (CDX-001..006) — SESI INI

- [x] CDX-001 `extractCodexThreadId(line)` + test (thread.started, session.created, legacy, null)
- [x] CDX-002 `isTurnEndLine(line)` + recognition `turn.*`/`thread.*`/`session.*` + test
- [x] CDX-003 OutputPump.threadId()/drainTurnEnded() + test
- [x] CDX-004 `sessionStore.ts` + test (persist, tags, search, cap 30×300, korup-JSON heal)
- [x] CDX-005 `buildCodexResumeArgs` + guard threadId + opsi `--search` + test
- [x] CDX-006 Mesin turn di AgentCodexView (running/idle, resume typing, autosave debounce)

## Milestone B — Studio UI (CDX-010..016) — SESI INI

- [x] CDX-010 `markdownLite.ts` + test XSS (script/onerror/javascript:) + konstruksi — **amend 2026-08-28: image `![alt](src)` https/data:image → `<img loading=lazy>`; src lain literal (test image baru)**
- [x] CDX-011 Studio chat (markdown assistant, kartu tool collapsible, banner error)
- [x] CDX-012 Session browser (search, filter tag, tag editor, lanjutkan/hapus)
- [x] CDX-013 Header live (thread badge, turn indicator, event count)
- [x] CDX-014 Composer hint resume saat idle; multiline gate tetap
- [x] CDX-015 Toggle web search di peluncur
- [x] CDX-016 Resume lintas-refresh (PTY baru + transcript dipulihkan)

## Milestone C — Profiles, Providers & Login (CDX-020..026) — menyusul

- [ ] CDX-020..026 (lihat spec 016 §5)

## Milestone D — MCP Manager (CDX-030..034) — menyusul

- [ ] CDX-030..034 (lihat spec 016 §6)

## Milestone E — Skills Manager (CDX-040..044) — menyusul

- [ ] CDX-040..044 (lihat spec 016 §7)

## Milestone F — Subagents (CDX-050..054) — menyusul

- [ ] CDX-050..054 (lihat spec 016 §8)

## Milestone G — Approvals & Permissions (CDX-060..062) — menyusul

- [ ] CDX-060..062 (lihat spec 016 §9)

## Definition of Done sesi ini

- [x] `npm test` (vitest) hijau semua (315 test)
- [x] `tsc && vite build` sukses
- [x] Multi-turn resume + persist lintas-refresh berfungsi (engine + wiring)
- [x] Test lama (drift, bridge, args) tidak rusak
