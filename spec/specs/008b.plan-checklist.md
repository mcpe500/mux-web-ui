# Companion Checklist — v0.5.2 Editor Multi-Terminal ala VS Code

- **Spec**: [`008.plan-v0.5.2-editor-multi-terminal-tabs.md`](./008.plan-v0.5.2-editor-multi-terminal-tabs.md)
- **Status**: Implemented — semua gate hijau; bukti test tercantum
- **TDD**: Red → Green per milestone; gate `cargo test` + `vitest` + `clippy -D warnings` + `fmt --check` + `npm run build ≤750 KiB` + `graphify update .`

---

## 1. Milestone A — Backend `--max-sessions` + metrics (`TAB-001`, `TAB-009`)

### Red (backend)
- [x] Red: `test_tab_001_default_limit_409_at_5` — default server: 4× POST `/terminals` → 201; ke-5 → `409 MAX_SESSIONS`.
- [x] Red: `test_tab_001_registry_configurable_limit` — `SessionRegistry::new_with(max_sessions=6)` unit: 6 sukses, ke-7 `MaxSessions` (pola `session.rs:404`).
- [x] Red: `test_tab_009_metrics_max_sessions` — `GET /metrics` body punya `max_sessions == 4`.

### Impl backend
- [x] `src/config.rs` — flag `--max-sessions` (env `MUX_WEB_MAX_SESSIONS`) default 4 + `effective_max_sessions()` clamp 1..=16.
- [x] `src/session.rs` — `SessionConfig.max_sessions: usize` (Default = 4); `create_session` pakai `self.config.max_sessions`; const MAX_SESSIONS tetap utk test/dokumentasi; update literal struct di `main.rs:124` dan unit-test `session.rs:422`.
- [x] `src/http.rs` — `MetricsResp.max_sessions` dari state config registry (wiring via `AppState`).
- [x] `src/main.rs` — pass `config.effective_max_sessions()`.

### Green
- [x] `cargo test --test session_tests test_tab*` hijau; `cargo test` **100%** (regresi EDT/V051 tetap).
- [x] clippy -D warnings 0 • fmt OK.

---

## 2. Milestone B — Logic murni (`terminalTabsLogic.ts`)

### Red (vitest — modul belum ada → gagal load)
- [x] Red: `createTab` — tambah ke grup aktif/grup pertama, jadi aktif; label unik (`bash`, `bash 2`); `maxReached` → return groups tanpa tab baru.
- [x] Red: `closeTab` — hapus + kembalikan `closedSessionId`; grup kosong kolaps; aktivasi pindah ke tetangga; menutup terakhir → groups kosong.
- [x] Red: `setActive` — key valid pindah aktif; key asing no-op aman.
- [x] Red: `moveTab` — reorder dalam grup; pindah antar grup; grup sumber kosong kolaps; index target dihormati (clamp out-of-range).
- [x] Red: `splitToNewGroup` — grup baru persis kanan sumber; flex sumber & baru 50/50 renormalisasi; tab jadi isi + aktif grup baru.
- [x] Red: `adjustGroupFlex` — clamp tiap fraksi 0.15..0.70 lalu renormalisasi Σ≈1.
- [x] Red: invariant helper — tidak ada grup tanpa tab; activeKey valid.

### Impl
- [x] `web/src/apps/editor/terminalTabsLogic.ts` lengkap sesuai spec §5 (immutable ops, counter key).

### Green
- [x] vitest terminalTabsLogic ≥16/16.

---

## 3. Milestone C — Wiring editor (`TextEditorView.tsx`)

### Impl
- [x] State `groups/panelOpen/spawningSeq`; hapus pemakaian `nextTerminalAction` untuk panel.
- [x] Header panel: tab strip draggable + `＋` (disable saat spawning/maxReached) + `⫿ split` + `↻ Restart` (per-tab aktif) + `✕ Hide`; label cwd aktif; banner V051 & cwd-legacy tetap.
- [x] Body: flex-row groups; divider antar-grup pointer-drag RAF → `adjustGroupFlex`; tiap grup render semua TerminalView miliknya mounted, visible hanya aktif (TAB-003).
- [x] DnD: `dragstart` set tabKey+fromGroup; drop pada grup/tab lain → `moveTab`; drop pada strip area kosong → grup baru.
- [x] Shortcut: Ctrl+` toggle; Ctrl+Shift+` spawn baru; Escape tak menutup panel terminal.
- [x] Restart per-tab: DELETE+POST cwd sama → update sessionId/workDir in-place.
- [x] EDT-009 dialog Replace menyebut jumlah terminal yang ditutup.
- [x] Metrics fetch (reuse health cycle atau /metrics sekali) untuk guard "N/max" tooltip.

### Green
- [x] `npm --prefix web test` pass • build gzip ≤750 KiB.
- [ ] Manual E2E checklist spec §C.3 — verifikasi nyata di Termux (user-side): buka 3 tab, split side-by-side, drag tab, tutup, panel auto-hide.

---

## 4. Final Gates & DoD

- [x] cargo test 100% • clippy 0 • fmt 0 • vitest hijau • budget PASS.
- [x] Versi 0.5.2: Cargo.toml + web/package.json.
- [x] `tests/traceability.md` TAB-001..009 → Passed.
- [x] graphify update . • handoff 005 • spec status Implemented.
- [ ] Tag `v0.5.2` → CI publish otomatis (permissions contents:write sudah ada) → `releases/latest`=v0.5.2, asset android ARM64 200.

---

## 5. Out of Scope
Rename manual, shell per-tab, grid 2×2, persist layout, touch-DnD penuh (spec §8-9).

## 6. Keputusan Log
| # | Keputusan | Status |
|---|---|---|
| D1 | Default limit tetap 4, configurable via flag | Decided |
| D2 | Split = instance BARU di grup kanan | Decided |
| D3 | Persist layout tunda | Deferred |
