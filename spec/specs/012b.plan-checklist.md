# Companion Checklist — v0.6.2 Terminal Resiliensi saat AFK (Auto-Reconnect + Retensi Sesi)

- **Spec**: [`012.plan-v0.6.2-terminal-afk-reconnect.md`](./012.plan-v0.6.2-terminal-afk-reconnect.md)
- **Status**: Implemented (2026-08-25) — smoke manual user-side pending
- **TDD**: Red → Green per milestone; gate `cargo test` + `clippy -D warnings` + `fmt --check` + `vitest` + `check_budget.sh` + `graphify update .`
- **Prinsip**: regresi nol (161 cargo + 70 vitest hijau tanpa edit); WS token tetap single-use TTL 10 s; satu socket aktif; budget ≤750 KiB gzip

---

## 0. Ringkasan Deliverable

| Area | File | Perubahan |
|---|---|---|
| Reconnect client | `web/src/apps/terminal/TerminalView.tsx` | refactor `attach()` reusable + backoff + guard satu socket + indicator |
| Kehadiran tab | `web/src/apps/terminal/TerminalView.tsx`, CSS | `visibilitychange`/`pagehide`; span `.term-reconnect` |
| Retention server | `src/config.rs`, `src/session.rs` | default `session_idle_timeout` 60→120 s + env override |
| Assurance | `tests/session_tests.rs`, `web/src/apps/terminal/*.test.ts(x)` | uji detach→reconnect tak re-reap; unit backoff/guard/stop-loop |

---

## 1. Milestone A — Auto-reconnect client (AFK-001..004, 007)

### Red
- [x] Vitest (unit, mock WebSocket): backoff sequence 500ms→1s→2s→4s→cap 10s.
- [x] Vitest: guard satu socket — panggil `attach()` saat socket lama masih OPEN memaksa close dulu.
- [x] Vitest: `/attach` 404 (SESSION_NOT_FOUND) → loop berhenti, overlay permanen, tidak retry.
- [x] Vitest: frame EXIT (opcode 0x03) → loop berhenti, tidak reconnect.

### Impl
- [x] `TerminalView.tsx`: ekstrak `attach()` reusable; pasang handler sama pada tiap siklus.
- [x] `ws.onclose` → hitung round, `setTimeout` backoff (cap 10 s); cleanup timer di `useEffect` cleanup.
- [x] Guard `socketRef` + `timerRef` (useRef): tutup socket lama sebelum open baru.
- [x] Indicator `<span className="term-reconnect">• Menghubungkan ulang…</span>` (ada di pojok selama retry),
      bukan overlay penuh; overlay permanen hanya untuk EXIT/404.

### Green & gates
- [x] Vitest 100% · budget PASS.

---

## 2. Milestone B — Kehadiran tab & keepalive (AFK-005..006)

### Red
- [x] Vitest: `visibilitychange→visible` saat socket putus langsung memicu reconnect (tanpa menunggu backoff).
- [x] Vitest: tab `hidden` membatalkan timer backoff; `visible` melanjutkan.
- [x] Vitest: `pagehide`/`beforeunload` menutup WS rapi dan membatalkan reconnect.

### Impl
- [x] Listener `visibilitychange` di `TerminalView.tsx`; hubungkan dengan state socket/timer.
- [x] Listener `pagehide`/`beforeunload` → cleanup socket + timer.
- [x] Pastikan PING→PONG (opcode 0x05→0x06) tetap ada (keepalive saat tab aktif).

### Green & gates
- [x] Vitest 100% · budget PASS.

---

## 3. Milestone C — Retention sesi server (AFK-009..011)

### Red
- [x] Cargo test baru: detach → sleep > `grace` kecil → reconnect lewat `attach_session` → pastikan
      `idle_deadline` clear & sesi tidak di-reap (AFK-010).
- [x] Cargo test: default `SessionConfig::default().grace_period == 120s` (AFK-009).

### Impl
- [x] `src/config.rs:63-64`: default `session_idle_timeout` 60 → 120; tambahkan env `MUX_WEB_SESSION_IDLE_TIMEOUT` override.
- [x] `src/session.rs:55`: `SessionConfig::default().grace_period` → `Duration::from_secs(120)`.
- [x] Verifikasi `test_sess_009_no_orphan_children_after_shutdown` tetap hijau (AFK-011).

### Green & gates
- [x] `cargo test` hijau · `clippy -D warnings` · `fmt --check`.

---

## 4. Verifikasi akhir

- [x] `cargo test` (161 + baru) · `clippy -D warnings` · `cargo fmt -- --check` · `vitest` · `check_budget.sh` PASS.
- [x] `graphify update .` (AST-only) dijalankan.
- [ ] Smoke manual (user-side): buka terminal → AFK (layar mati) beberapa menit → kembali → auto-reconnect dengan riwayat
      ring replay, tanpa overlay permanen.
- [ ] Smoke manual (user-side): proses pekerjaan (mis. codex) masih berjalan setelah putus-koneksi & reconnect.
- [x] `spec/specs/012.plan-*.md` & `012b` diperbarui statusnya sesuai progres.

---

## 5. Catatan

- Perubahan perilaku default yang disengaja hanya `session_idle_timeout` 60 → 120 s (+ env override).
- Dikembalikan ke `=0` tetap berarti reap nonaktif (tidak mengubah SESS-010 semantics).