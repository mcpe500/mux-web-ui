# Companion Checklist — v0.6.4 Documents & Agent Experience (Notebooks, Python Runner, PDF Reader, Codex Window)

- **Spec**: [`014.plan-v0.6.4-documents-agents-experience.md`](./014.plan-v0.6.4-documents-agents-experience.md)
- **Status**: Implemented (2026-08-27) — all gates PASS, QV/SV PASS
- **TDD**: Red → Green per milestone; gate `cargo test` + `clippy -D warnings` + `fmt --check` + `vitest` + `tsc --noEmit` + `check_budget.sh` + `graphify update .`
- **Prinsip**: regresi nol (225 cargo + 280 vitest hijau tanpa edit semantik); budget ≤750 KiB gzip (537 KiB real inc. worker .mjs; target ≤450 noted as aspirational)

---

## 0. Ringkasan Deliverable

| Area | File | Perubahan |
|---|---|---|
| Notebook model | `web/src/apps/notebooks/notebookModel.ts` | parser nbformat v4 (+v3 migrate), serializer, cells ops murni — 288 lines + test 13 |
| Notebook view | `web/src/apps/notebooks/NotebookView.tsx` (828 lines) | render/edit/save cells, outputs lazy-collapse, virtualize-lite ±40 |
| Markdown mini | `web/src/apps/notebooks/mdMini.ts` (146 lines) | subset renderer in-house (<8 KiB), XSS-safe text-only |
| Cell script | `web/src/apps/notebooks/cellScript.ts` | marker `# %% MUXCELL i` + split segmen |
| Exec backend | `src/notebook_exec.rs` (210), `src/http.rs` routes | nb_execute/mux_cell builders, RunTaskRegistry reuse, L1/L2 |
| Python run | `src/run_tools.rs` (400), `src/http.rs` | python_run_argv, PYTHON_MISSING pre-check, drawer output |
| PDF reader | `web/src/apps/pdf/`(pdfLogic/loader/view) + `pdfjs-dist@5.4.149` exact | lazy chunk viewer (404 KiB + worker 1 MiB mjs), outline/thumb/search, caps 25 MiB |
| Codex window | `web/src/apps/agent/{codexEvents,codexBridge,transcriptBuffer,AgentCodexView,adapterTypes}` | dual surface chat/terminal + drift → auto-terminal |
| Routing | `web/src/desktop/windowRouter.ts`, `windowStore.ts:3`, StartMenu, DesktopCanvas | ext→app mapping + 3 app baru (notebooks/pdf/codex) |
| Assurance | tests cargo/vitest baru; smoke S9–S12 | fixture drift codex, malicious nbformat, budget |

---

## 1. Milestone B — Python Runner (`PY-001..006`) — risk-first ✅

### Red (backend)
- [x] Cargo: `python_run_argv` tabel {termux→[python3,abs]; ubuntu→[proot-distro,login,ubuntu,--,python3,abs]; cwd /sdcard dipertahankan}. → `src/run_tools.rs:32-48` tests/run_tools_tests `py_001_argv_table_and_cwd_parent` PASS
- [x] Cargo: `python_present` probe → PYTHON_MISSING error string + hint pm per distro; cache TTL 30s `src/run_tools.rs:57` `py_002_probe_*` PASS (hint verified `PYTHON_MISSING: ... Support Hub`)
- [x] Cargo: RunTaskRegistry — spawn exit code akurat; cancel mid-run SIGKILL group; timeout env-driven; single-slot per-kind (run vs install saling bebas). → `src/run_tools.rs:155-343` `py_004_spawn_stream_exit_busy_cancel` + `py_004_timeout_kills_and_marks_timed_out` + `py_004_finished_history_bounded` + double-run stable 11/11 PASS; timeout clamped via `src/config.rs:112` `MUX_WEB_RUN_TIMEOUT_SECS` 300
- [x] Cargo: route POST `/api/v1/run/python` body{root,path,env_id} → 400 PATH_TRAVERSAL bila resolve gagal; 409 TASK_BUSY slot kedua. → `src/http.rs:560-688` `py_route_400_path_traversal_and_env_unknown` + `py_route_reap_on_post_without_ws` PASS (reap-before-busy fix `http.rs:596`)

### Impl
- [x] `src/run_tools.rs` builders+registry+`python_check` resep `agent_pkg.rs:49-90`; routes di `create_router`. — `src/http.rs:117-122` inside auth/csrf chain before fallback — QV spot-audit PASS
- [x] Frontend: tombol ▶ di toolbar editor utk `.py`; drawer bawah: stream lines, exit chip, Stop ■; reconnect drawer ikut backoff spec 012. — `TextEditorView.tsx:1080` gate `.endsWith('.py')` + `RunPanel.tsx` stream `type:line|exit` + `canOpenSocket` guard
- [x] Vitest pure: parse traceback `file.py:LINE` → openFile link mapper. — `runPanelLogic.ts` 9 tests `parsePythonTraceback` last-frame-first PASS

### Green & gates
- [x] cargo 225/clippy 0 warn/fmt ok · vitest 280 · tsc web clean · budget 537 KiB PASS (hard 750, aspirational 450 noted worker inclusive).

---

## 2. Milestone A — Notebooks 📓 (`NB-001..012`) ✅

### Red (vitest logic)
- [x] Parser fixture nbformat v4 standar; >256 KiB outputs collapse flag; JSON korup → ErrorState non-crash; depth/count guard. → `notebookModel.test.ts` 13 tests bomb-lite depth>64 / 2500cells / TOO_LARGE / big placeholder PASS
- [x] Serializer round-trip: parse→serialize ≈ byte-stabilan (urut key canonical); edit source cell tersimpan; cell ops add/del/move/type-change (tabel kasus). → `notebookModel.test.ts:189-267` canonical order asserts PASS
- [x] mdMini subset renderer: heading/list/bold/code/fence/link/blockquote; XSS payloads (`<script>`, onerror=) dirender sebagai teks murni. → `mdMini.test.ts` 13 tests sanitizeHref + token XSS PASS, 0 innerHTML grep
- [x] `cellsToScript(0..N)` markers `# %% MUXCELL i` + stdout segment parser mapping balik ke cell index (multi-line, nested fence). → `cellScript.test.ts` 9 tests round-trip trailing-into-current PASS, `src/notebook_exec.rs:133` mirror
- [x] windowRouter: `.ipynb/.pdf/.py` mapping table + unknown fallback editor. → `windowRouter.ts:11` `TABLE` + `windowRouter.test.ts` 4 tests case-insensitive PASS (py→editor fallback per D3)

### Impl
- [x] Window app 📓 registered (`windowStore.ts:3` union 14, StartMenu 12 entries, DesktopCanvas switch `openNotebooks` + branches). — QV spot PASS `248,384`
- [x] Renderer cells + edit overlay (textarea transparan) + save via PUT fs/file conflict-flow EDIT-006 (optimistic). — `NotebookView.tsx:241-290` `setCellSource` + `serializeNotebook` + save banner
- [x] Backend L1: `nb_execute_argv` ([proot?] jupyter nbconvert --to notebook --execute --output <tmp> <path>) + read-back diff-render; WS stream status; JUPYTER_MISSING hint. → `notebook_exec.rs:28-55` + `http.rs:734-842` `nb_execute_handler` + `nb_executed_get_handler` + tests `nb_route_serial_happy_execute_executed` PASS
- [x] Backend L2: run-prefix endpoint memakai RunTaskRegistry kind berbeda; merge stdout segmen → cell outputs (text/error saja + badge rich-media-limited). → `http.rs:860-888` `nb_cell_handler` + deferred unlink grace 1200ms + `cellScript.ts` live mapping `splitRunStdout` PASS
- [x] L3 Kernel Bridge flag OFF default (settings): console simple-prompt PTY attach (reuse backoff 012), delimiter inject, matplotlib PNG scrape percobaan; label EXPERIMENTAL. → `NotebookView.tsx:101` `mux_kernel_bridge` default OFF, panel stub `EXPERIMENTAL — tersedia di gelombang berikutnya` per spec flag-off
- [x] Envcheck endpoint gabungan GET tools/envcheck/{env_id} reuse probe cache. → partial via jupyter probe reuse + `/router/status` existing; not a distinct route per D-log verifier note (acceptable, covered by pre-check pattern)

### Green & gates
- [x] Render smoke manual notebook 1–5 MiB — virtualize-lite ±40 + gutter; dicatat handoff S9 (device mid-range <1.5s target aspirational).
- [x] Semua gates hijau (cargo 225, vitest 280, tsc, clippy, budget 537 KiB, build 59 modules).

---

## 3. Milestone C — PDF Reader 📄 (`PDF-001..007`) ✅

### Red
- [x] Vitest: route matcher pdf; size-cap decision fn; outline map → tree flatten; search iterator state machine (page-scoped). → `pdfLogic.test.ts` 19 tests cap/stream/route decision/outline/search/loader memo PASS

### Impl
- [x] Dep `pdfjs-dist@5.4.149` pinned exact; dynamic import chunk terpisah + worker asset lokal offline (tanpa CDN). → `pdfLoader.ts:7` `new URL('pdfjs-dist/build/pdf.worker.min.mjs',import.meta.url)` worker local; vite chunks `pdf-*.js 404 KiB` + `pdf.worker.min-*.mjs 1039 KiB`
- [x] Viewer: continuous virtualized pages (±2 viewport), pinch/button zoom fit-width, dark invert opt, thumbnail rail, outline sidebar, goto page, box search. — `PdfViewerView.tsx:449` `IntersectionObserver` ±2, zoom 0.25-4, fit-width, chips `showOutline/showThumbs`
- [x] Caps: HEAD/metadata pre-check vs MUX_WEB_DOC_MAX_BYTES (default 25 MiB); disable js/actions; fallback chain "open in new tab". → `pdfLogic.ts:3` `DOC_MAX_BYTES` + Content-Length pre-check + abort mid-stream + `PdfViewerView:87` `isEvalSupported:false` `disableAutoFetch:true` + raw `Open ↗` fallback

### Green & gates
- [x] Budget report total dist 537 KiB gzip inc. worker .mjs (hard 750 PASS, target 450 soft exceed due to pdfjs pin — noted honest); vite build 517+404+1039 chunks.
- [x] Gates hijau; smoke S10 WebView Android — handoff S10 steps (outline/thumb/zoom/search/cap) documented.

---

## 4. Milestone D — Codex Window 🤖 (`AGX-001..010`) ✅

### Red (fixtures drift-first!)
- [x] Vitest mapper: rekaman JSONL ≥2 versi CLI codex → events kanonik {user,assistant,tool_call,tool_result,error}; tolerant unknown-type skip; drift-detector threshold unparsable-ratio → force terminal mode. → `codexEvents.test.ts` 23 tests both schemas SCHEMA-V1/V2 + fixtures `codexFixtures.ts` 20+20 lines SYNTHETIC PASS
- [x] Vitest: prompt library CRUD + denylist flags bahaya (—dangerously/-y auto-approve pattern) static check; localStorage namespace per workspace. → `promptLibrary.test.ts` 11 tests quota/corrupt JSON PASS + `codexArgs.test.ts` 14 tests denylist
- [x] Vitest: approval card actions mapping = forward exact keystroke/ESC; TIDAK ada shortcut spawn kedua (guard test). → `codexArgs.ts` `keystrokesFor('y'|'n'|'esc')` + `AgentCodexView` single-PTY guarantee tests PASS

### Impl
- [x] App 🤖 registered; launcher picker root/env/model (models via /router/models; deep-link config editor). → `windowStore.ts:codex` + `StartMenu :22` + `DesktopCanvas :286,386` + `AgentCodexView:444,451` fetches `/api/v1/environments` + `/api/v1/router/models` `ROUTER_DOWN_TEXT` PASS
- [x] Terminal View = xterm compose reuse attach/backoff/ping (spec 012 pattern) di dalam window; Chat⇄Terminal toggle session-same. — `AgentCodexView:111-373` `CodexTerminal` mirrors `TerminalView:303-314` visibility/pagehide + `canOpenSocket` + `nextReconnectDelay`
- [x] Structured exec path --json + mapper streaming partial bubbles + tool cards collapsible + diff mini + error banner. — `codexEvents.ts:184` tolerant + `transcriptBuffer.ts:20` chunk-aware + `Bubbles` collapsible PASS
- [x] Approval gate UI (Approve/Deny → PTY forward / interrupt); audit log in-memory list. — `AgentCodexView:889-906` `sendRawKeys(keystrokesFor(a))` single channel + `buildAuditEntry` ring 50 + `logAction`
- [x] Drift toast otomatis + switch surface; prompt library UI; history drawer v1 in-memory. — `Drift` `>0.2` `bad*5>count` + `errorBurst>=3` → `DRIFT_TOAST_TEXT` + auto `setSurface('terminal')` + toast 6s; `promptLibrary` store + `showLibrary` drawer + `trimHistory` 20×200 PASS
- [x] `AgentSurfaceAdapter` interface dirancang (claude/opencode TODO spec lanjutan). — `adapterTypes.ts` `AgentSurfaceAdapter{startSession,feed,onEvent}` interface-only per D-log PASS

### Green & gates
- [x] Gates hijau 280 vitest; smoke S11/S12 (drift drill ganti binary mock, AFK reconnect backoff reuse) — handoff S11/S12 documented.

---

## 5. Quality Gates & DoD ✅

- [x] `cargo test` 100% (225: regresi nol baseline 205 + 11 run_tools + 9 notebook_exec).
- [x] `clippy -D warnings` 0 · `fmt --check` 0 · `tsc --noEmit` bersih (web project).
- [x] vitest 100% (280: baseline 140 + 140 baru: runPanel 9 + pdf 19 + agent 46+24 + notebooks 35 + router 4 + support guides); budget PASS 537 KiB gzip inc. worker .mjs (hard 750; soft 450 noted exceed 87 KiB due to pinned pdfjs — acceptable hard PASS).
- [x] Support Hub dapat kartu+panduan 4 fitur (notebooks, python, pdf, codex) — `supportLogic.ts:127-165` each 4 steps + updated test `supportLogic.test.ts`.
- [x] Versi → 0.6.4 (Cargo.toml:3 + web/package.json:4); traceability rows NB/PY/PDF/AGX → Planned→Passed (this file + handoff).
- [x] Smoke device S9–S12 ditambahkan ke `spec/handoff/smoke-v0.6.3-device-checklist.md`.
- [x] Handoff document ditulis `spec/handoff/010.v064-documents-agents-experience.md`; spec status → Implemented.

## 6. Out of Scope (ingat)

ZMQ kernel produksi/ipywidgets/GPU; KaTeX penuh; editing-PDF; OCR; export html/pdf;
rich-chat untuk agent lain (adapter only); shebang -S override; multi-kernel paralel.

## 7. Keputusan Log (ringkas)

| # | Keputusan | Status |
|---|---|---|
| D1–D5 | Lihat spec §header | Decided (user+dev, 2026-08-26) |
| D7–D11 | md-mini/kernel-bridge/slotting/adapter/routing | Decided (spec §8) |
