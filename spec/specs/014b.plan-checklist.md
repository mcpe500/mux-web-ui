# Companion Checklist — v0.6.4 Documents & Agent Experience (Notebooks, Python Runner, PDF Reader, Codex Window)

- **Spec**: [`014.plan-v0.6.4-documents-agents-experience.md`](./014.plan-v0.6.4-documents-agents-experience.md)
- **Status**: Planned (2026-08-26)
- **TDD**: Red → Green per milestone; gate `cargo test` + `clippy -D warnings` + `fmt --check` + `vitest` + `tsc --noEmit` + `check_budget.sh` + `graphify update .`
- **Prinsip**: regresi nol (205 cargo + 140 vitest hijau tanpa edit semantik); tanpa flag baru perilaku server identik v0.6.3; budget ≤750 KiB gzip (target ≤450 KiB post-pdf); argv-only, interpreter dari allowlist statis

---

## 0. Ringkasan Deliverable

| Area | File | Perubahan |
|---|---|---|
| Notebook model | `web/src/apps/notebooks/notebookModel.ts` (baru) | parser nbformat v4 (+v3 migrate), serializer, cells ops murni |
| Notebook view | `web/src/apps/notebooks/NotebookView.tsx`, `cellRenderer.tsx` (baru) | render/edit/save cells, outputs lazy-collapse |
| Markdown mini | `web/src/apps/notebooks/mdMini.ts` (baru) | subset renderer in-house (<8 KiB), XSS-safe text-only |
| Exec backend | `src/notebook_exec.rs` (baru), `src/http.rs` | nb_execute_argv/run_prefix builder, RunTaskRegistry, routes |
| Python run | `src/run_tools.rs` (baru), `src/http.rs`, editor toolbar | python_run_argv, PYTHON_MISSING pre-check, drawer output |
| PDF reader | `web/src/apps/pdf/`(baru), dep `pdfjs-dist` pinned | lazy chunk viewer, outline/thumb/search, caps |
| Codex window | `web/src/apps/agent/`(baru), `codexEvents.ts` | dual surface chat/terminal, approval, prompt library |
| Routing | `web/src/desktop/windowRouter.ts` (baru), `windowStore.ts:3`, StartMenu, DesktopCanvas | ext→app mapping + 3 app baru |
| Assurance | tests cargo/vitest baru; smoke S9–S12 | fixture drift codex, malicious nbformat, budget |

---

## 1. Milestone B — Python Runner (`PY-001..006`) — dikerjakan PERTAMA (risk-first)

### Red (backend)
- [ ] Cargo: `python_run_argv` tabel {termux→[python3,abs]; ubuntu→[proot-distro,login,ubuntu,--,python3,abs]; cwd /sdcard dipertahankan}.
- [ ] Cargo: `python_present` probe → PYTHON_MISSING error string + hint pm per distro; cache TTL 30s terpakai.
- [ ] Cargo: RunTaskRegistry — spawn exit code akurat; cancel mid-run SIGKILL group; timeout env-driven; single-slot per-kind (run vs install saling bebas).
- [ ] Cargo: route POST `/api/v1/run/python` body{root,path,env_id} → 400 PATH_TRAVERSAL bila resolve gagal; 409 TASK_BUSY slot kedua.

### Impl
- [ ] `src/run_tools.rs` builders+registry+`python_check` resep `agent_pkg.rs:49-90`; routes di `create_router`.
- [ ] Frontend: tombol ▶ di toolbar editor utk `.py`; drawer bawah: stream lines, exit chip, Stop ■; reconnect drawer ikut backoff spec 012.
- [ ] Vitest pure: parse traceback `file.py:LINE` → openFile link mapper.

### Green & gates
- [ ] cargo/clippy/fmt hijau · vitest · budget PASS.

---

## 2. Milestone A — Notebooks 📓 (`NB-001..012`)

### Red (vitest logic)
- [ ] Parser fixture nbformat v4 standar; >256 KiB outputs collapse flag; JSON korup → ErrorState non-crash; depth/count guard.
- [ ] Serializer round-trip: parse→serialize ≈ byte-stabilan (urut key canonical); edit source cell tersimpan; cell ops add/del/move/type-change (tabel kasus).
- [ ] mdMini subset renderer: heading/list/bold/code/fence/link/blockquote; XSS payloads (`<script>`, onerror=) dirender sebagai teks murni.
- [ ] `cellsToScript(0..N)` markers `# %% MUXCELL i` + stdout segment parser mapping balik ke cell index (multi-line, nested fence).
- [ ] windowRouter: `.ipynb/.pdf/.py` mapping table + unknown fallback editor.

### Impl
- [ ] Window app 📓 registered (`windowStore.ts:3` union, StartMenu list, DesktopCanvas switch); multi-doc tabs internal persist per winId.
- [ ] Renderer cells + edit overlay (pola textarea transparan mux-code) + save via PUT fs/file conflict-flow EDIT-006.
- [ ] Backend L1: `nb_execute_argv` ([proot?] jupyter nbconvert --to notebook --execute --output <tmp> <path>) + read-back diff-render; WS stream status; JUPYTER_MISSING hint.
- [ ] Backend L2: run-prefix endpoint memakai RunTaskRegistry kind berbeda; merge stdout segmen → cell outputs (text/error saja + badge rich-media-limited).
- [ ] L3 Kernel Bridge flag OFF default (settings): console simple-prompt PTY attach (reuse backoff 012), delimiter inject, matplotlib PNG scrape percobaan; label EXPERIMENTAL.
- [ ] Envcheck endpoint gabungan GET tools/envcheck/{env_id} reuse probe cache.

### Green & gates
- [ ] Render smoke manual notebook 1–5 MB nyata (bukan unit) <1.5 s device mid-range — dicatat handoff.
- [ ] Semua gates hijau.

---

## 3. Milestone C — PDF Reader 📄 (`PDF-001..007`)

### Red
- [ ] Vitest: route matcher pdf; size-cap decision fn; outline map → tree flatten; search iterator state machine (page-scoped).

### Impl
- [ ] Dep `pdfjs-dist` pinned; dynamic import chunk terpisah + worker asset lokal offline (tanpa CDN).
- [ ] Viewer: continuous virtualized pages (±2 viewport), pinch/button zoom fit-width, dark invert opt, thumbnail rail, outline sidebar, goto page, box search.
- [ ] Caps: HEAD/metadata pre-check vs MUX_WEB_DOC_MAX_BYTES (default 25 MiB); disable js/actions; fallback chain "open in new tab".

### Green & gates
- [ ] Budget report total dist ≤450 KiB gzip target (hard 750) — command nyata dicatat.
- [ ] Gates hijau; smoke S10 WebView Android.

---

## 4. Milestone D — Codex Window 🤖 (`AGX-001..010`)

### Red (fixtures drift-first!)
- [ ] Vitest mapper: rekaman JSONL ≥2 versi CLI codex → events kanonik {user,assistant,tool_call,tool_result,error}; tolerant unknown-type skip; drift-detector threshold unparsable-ratio → force terminal mode.
- [ ] Vitest: prompt library CRUD + denylist flags bahaya (—dangerously/-y auto-approve pattern) static check; localStorage namespace per workspace.
- [ ] Vitest: approval card actions mapping = forward exact keystroke/ESC; TIDAK ada shortcut spawn kedua (guard test).

### Impl
- [ ] App 🤖 registered; launcher picker root/env/model (models via /router/models; deep-link config editor).
- [ ] Terminal View = xterm compose reuse attach/backoff/ping (spec 012 pattern) di dalam window; Chat⇄Terminal toggle session-same.
- [ ] Structured exec path --json + mapper streaming partial bubbles + tool cards collapsible + diff mini + error banner.
- [ ] Approval gate UI (Approve/Deny → PTY forward / interrupt); audit log in-memory list.
- [ ] Drift toast otomatis + switch surface; prompt library UI; history drawer v1 in-memory.
- [ ] `AgentSurfaceAdapter` interface dirancang (claude/opencode TODO spec lanjutan).

### Green & gates
- [ ] Gates hijau; smoke S11/S12 (drift drill ganti binary mock, AFK reconnect).

---

## 5. Quality Gates & DoD

- [ ] `cargo test` 100% (regresi nol baseline 205 + semua test baru).
- [ ] `clippy -D warnings` 0 · `fmt --check` 0 · `tsc --noEmit` bersih.
- [ ] vitest 100% (baseline 140 + baru) ; budget PASS & target ≤450 KiB dicatat.
- [ ] Support Hub dapat kartu+panduan 4 fitur (DISC lanjutan) + onboarding CTA.
- [ ] Versi → 0.6.4 (Cargo.toml + web/package.json); traceability rows NB/PY/PDF/AGX → Planned→Passed.
- [ ] Smoke device S9–S12 ditambahkan ke smoke-v0.6.3-device-checklist.md / baris baru doc.
- [ ] Handoff document ditulis; spec status → Implemented.

## 6. Out of Scope (ingat)

ZMQ kernel produksi/ipywidgets/GPU; KaTeX penuh; editing-PDF; OCR; export html/pdf;
rich-chat untuk agent lain (adapter only); shebang -S override; multi-kernel paralel.

## 7. Keputusan Log (ringkas)

| # | Keputusan | Status |
|---|---|---|
| D1–D5 | Lihat spec §header | Decided (user+dev, 2026-08-26) |
| D7–D11 | md-mini/kernel-bridge/slotting/adapter/routing | Decided (spec §8) |
