# Mux Web UI — v0.3 Implementation Checklist & Verification Matrix

Status: Completed (Implemented & Verified)
Companion to: [`003.plan-v0.3-terminal-resize.md`](./003.plan-v0.3-terminal-resize.md)
Rule: Every task requires TDD Red -> Green -> Refactor execution. Verified by automated unit/integration tests.

---

## TDD Execution Loop Standard

For every item:
1. **Red**: Write a failing test before writing production code.
2. **Green**: Write minimal code to pass the test.
3. **Refactor**: Clean up while keeping tests green.
4. **Verify**: Run full test suite (`cargo test`, `npm test`) and clippy checks.

---

## Langkah 1 — Milestone A: Backend Dimension Clamping & Robustness

- [x] **A.1** Unit tests in `src/pty.rs` for dimension clamping constants and helper `clamp_dimensions` (`RSZ-002`) — `pty::tests::test_rsz_002_clamp_dimensions_bounds`
- [x] **A.2** Implementation of `clamp_dimensions` and updating `PtySession::new` and `PtySession::resize` (`RSZ-002`) — `src/pty.rs:65, 290, 370`
- [x] **A.3** Integration tests in `tests/resize_tests.rs`:
  - [x] `test_rsz_001_create_terminal_dimension_clamping` (`RSZ-003`)
  - [x] `test_rsz_002_ws_resize_frame_updates_pty_and_metadata` (`RSZ-009`)
  - [x] `test_rsz_003_ws_resize_extreme_values_clamped` (`RSZ-002`)
  - [x] `test_rsz_004_ws_resize_on_closed_session_handled_cleanly` (`RSZ-005`, `RSZ-006`)
  - [x] `test_rsz_005_rapid_resize_frame_flood_stability` (`RSZ-004`, `RSZ-010`)
- [x] **A.4** Wire `clamp_dimensions` and safe handling in `src/http.rs` (`create_terminal_handler` and `handle_terminal_ws`) — `src/http.rs:368, 658`

## Langkah 2 — Milestone B: Frontend Window Resize Handles & Debouncing

- [x] **B.1** Vitest unit tests in `web/src/desktop/windowStore.test.ts` for bounds update & dimension constraints (`RSZ-008`)
- [x] **B.2** Implement 8-way resize handles & corner grip with Pointer Events in `web/src/desktop/WindowFrame.tsx` (`RSZ-008`)
- [x] **B.3** Add CSS styles and cursors for resize handles in `web/src/styles/main.css` (`RSZ-008`)
- [x] **B.4** Implement trailing-edge debouncer & zero-dimension check in `web/src/apps/terminal/TerminalView.tsx` (`RSZ-006`, `RSZ-007`)
- [x] **B.5** Verify frontend builds and passes tests (`npm --prefix web test && npm --prefix web run build`)

## Langkah 3 — Milestone C: Traceability, Documentation & Graph Update

- [x] **C.1** Update `tests/traceability.md` with `RSZ-001` through `RSZ-010` marked as `Passed`
- [x] **C.2** Run `cargo clippy --all-targets -- -D warnings` and `cargo fmt --check`
- [x] **C.3** Run `graphify update .` to synchronize knowledge graph
- [x] **C.4** Complete Definition of Done audit
