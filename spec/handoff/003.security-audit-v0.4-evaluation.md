# Handoff 003 — Security Audit v0.3 & Evaluasi v0.4 Developer Suite

Status: **Audit v0.3 selesai (read-only, 7 thought), spec v0.4 diperbarui dengan Hardening Gate PRE + phasing Opsi A. Belum diimplementasi — siap untuk Build.**
Tanggal: 2026-08-21
Companion: [`../specs/004.plan-v0.4-suite.md`](../specs/004.plan-v0.4-suite.md) (updated 2026-08-21, §1.2 + §3.2 + §9 + §12-15), [`../specs/004b.plan-checklist.md`](../specs/004b.plan-checklist.md) (updated, Milestone PRE `§0` + DoD per fase), [`../specs/003.plan-v0.3-terminal-resize.md`](../specs/003.plan-v0.3-terminal-resize.md) (Implemented)
Previous: [`002.terminal-resize-completed.md`](./002.terminal-resize-completed.md) (v0.3, 78 tests + Vitest hijau, pushed `main`)
Graph: `graphify-out/GRAPH_REPORT.md` 736 nodes / 1286 edges / 37 communities, commit `1592b060` (2026-08-19, stale — refresh planned post-handoff)

---

## 1. Status Ringkas

- **Audit v0.3 (read-only, Plan Mode):** `graphify query` + inspeksi `src/http.rs:695`, `src/auth.rs:343`, `src/paths.rs:332`, `src/pty.rs:394`, `src/session.rs:503`, `src/protocol.rs:184`, `src/config.rs:89`, `src/main.rs:231`, `install.sh:206`, `update.sh:177`, `.github/workflows/ci.yml:269`. 7 sequential-thinking calls (Plan Mode wajib ≥5).
- **Temuan:** 2 HIGH (H1 `rand_id` nanos, H2 hardcode IP), 5 MEDIUM (M1 headers, M2 CSRF, M3 `constant_time_eq`, M4 supply chain, M5 cert 2030), 3 LOW — dirangkum `004.plan-v0.4-suite.md:§1.2` dengan `source_location` + tabel kekuatan terverifikasi.
- **Evaluasi v0.4:** Visi "Complete Developer Environment in Your Browser" (`004:§Brand`) **cocok 85%** tapi **belum sempurna** — skop monolitik 32 req + 4 service + 4 frontend apps terlalu besar untuk `http.rs:695` cohesion `0.11`. Rekomendasi **Opsi A Iterative** (PRE → v0.4.0 Archive+Share → v0.4.1 Git → v0.4.2 Package non-sudo) — `§12.4` dipilih, Opsi B monolit fallback.
- **Spec update:** `004.plan-v0.4-suite.md` + `004b.plan-checklist.md` sudah diperkaya audit + phasing + `SEC-004..006` + `§9` Hardening Gate + `§14` graph sync. Belum ada perubahan kode (`cargo test`/`clippy`/`fmt` belum dijalankan di Build ini — handoff harapkan next agent jalankan).
- **Keputusan user:** Opsi A disetujui implisit ("oke lengkapi ... jadi semua"), jadi spec ditulis untuk Opsi A. `Package sudo` ditunda ke `v0.5` per `§12.4` (konfirmasi eksplisit disarankan).
- **Graph:** `graphify update .` dicatat di `§14` per `AGENTS.md:19` — akan dijalankan setelah handoff (AST + semantic `/graphify --update` untuk docs).

---

## 2. Temuan Audit — Aksi Wajib

### HIGH (Blokir v0.4 Fitur)

- **H1 — `src/session.rs:367-374` `rand_id()` nanos prediktabil** → ganti `getrandom` 16-32 byte hex; audit `term-`/`tok-`/`att-` semua callsite. Test burst 100 concurrent.
- **H2 — `src/http.rs:148` hardcode `127.0.0.1`** → real IP via `ConnectInfo<SocketAddr>` ke `auth.pair()`; `test_auth_004` pakai IP nyata.

### MEDIUM (Blokir Rilis Publik)

- **M1 — Security headers** (`src/http.rs:472` hanya `nosniff`) → middleware `security_headers` global (CSP, X-Frame DENY, HSTS jika TLS, Referrer).
- **M2 — CSRF Origin-only** (`src/http.rs:288-320`) → double-submit `__Host-csrf` cookie + `X-CSRF-Token` untuk POST/PUT/DELETE.
- **M3 — `src/auth.rs:215-225` `constant_time_eq` → `subtle` crate.**
- **M4 — Supply chain `install.sh:96`/`update.sh:111` `curl grep`** → enforce `curl -fsSL`, doc `MUX_WEB_BASE_URL` scope, catat `cosign` untuk v0.5.
- **M5 — Cert 2030 tanpa rotation** (`src/main.rs:65`) → startup warning `<30d` + docs rotation.

### LOW

- L1 cookie `__Host-` mismatch proxy, L2 `data_dir` writability, L3 `ring` vs `aws-lc-rs` — catat, tidak blok.

---

## 3. v0.4 — Evaluasi & Phasing Baru

**Mau apa:** 4 pilar `GIT`/`PKG`/`SHR`/`ARC` (`004:§2`).

**Cocok?** Ya untuk fragmentasi workflow `§1:23-36`.

**Sempurna?** Belum — monolitik 32 req + `http.rs` 695 baris cohesion 0.11 → **pecah Opsi A:**

| Fase | Scope | Gate |
|---|---|---|
| **PRE** | Hardening Gate `§9` (H1,H2,M1,M2,M3, split `routes/*`) | `SEC-004..006` Passed |
| **v0.4.0** | Archive + Share (isolatable, tanpa privilege) | `ARC-001..007` + `SHR-001..006` |
| **v0.4.1** | Git UI (`GIT-001..008`) | No-shell + sandbox |
| **v0.4.2** | Package Center non-sudo (`PKG-001..006`) | `sudo` tunda v0.5 |

Opsi B monolit simpan sebagai fallback tim besar.

---

## 4. Sisa Pekerjaan (Next Agent — Build Mode)

1. **Implementasi PRE** (`004b:§0`): `tests/security_tests.rs` 5 Red → Green per tabel `§9.1`; `subtle` dep; `ConnectInfo`; `security_headers`; `csrf_guard`; split `src/routes/*`; `cargo test` + `clippy -D warnings` + `fmt --check` + `graphify update .`.
2. **v0.4.0** (`004b:§3-4`): `archive_tests` 8 + `share_tests` 6 — `ARC`/`SHR`.
3. **v0.4.1** (`004b:§1`): `git_tests` 8 — `GIT`.
4. **v0.4.2** (`004b:§2`): `package_tests` 5 — `PKG` (non-sudo).
5. **Frontend** (`004b:§5`) + **DoD** (`004b:§6`) per fase + final.
6. **Graph:** `graphify update .` per fase + `/graphify --update` untuk docs substansial (handoff ini).

**Urutan TDD detail:** `004.plan-v0.4-suite.md:§13.1-13.6`.

---

## 5. Rekomendasi Urutan & Catatan Teknis

- **Jangan mulai fitur sebelum PRE hijau** — gate rule `004b` header.
- **`http.rs` split** dulu agar diff review v0.4 kecil; target cohesion naik dari `0.11`.
- **Package sudo:** konfirmasiuser apakah `v0.4.2` benar non-sudo; jika butuh sudo, desain PTY runner harus explicit consent + log.
- **CI:** `ci.yml:269` matrix 4 target tetap; tambah job `security-tests` jika perlu.
- **Traceability:** `tests/traceability.md:149-172` `Planned` → update per fase (`SEC-004..006` baru).
- **Gotcha 001 §3.2:** fd-leak `close_range` tetap valid — jangan regresi saat refactor routes/pty.
- **Graph dirty = normal** (`AGENTS.md:5`) — `graphify-out/.rebuild.lock` transient `.gitignore`.

---

## 6. Verifikasi Handoff Ini

- `004.plan-v0.4-suite.md:§1.2` memuat 10 temuan (H1-M5-L3) dengan `source_location`.
- `§3.2` + `§9` + `§9.2` (`SEC-004..006`) adalah translasi temuan → task.
- `§12` evaluasi 85% cocok / gap / Opsi A/B + keputusan Opsi A.
- `§14` graph sync + `§15` DoD hardening-incorporated.
- `004b.plan-checklist.md:§0` PRE 5 Red + 6 impl task + gate; `§1-5` di-reorder ke v0.4.0/1/2.
- `graphify update .` belum dijalankan di handoff write — dijadwalkan Build step berikutnya (AST-only, no API cost).

---

*Handoff siap — next agent mulai Build dari `§0` PRE. Jalankan `graphify update .` setelah PRE, lalu commit dengan `feat(security): harden session tokens and security headers (H1,M1)`.*
