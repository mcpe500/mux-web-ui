# 004 — Handoff: Hotfix v0.5.1 Terminal CWD & Prompt Rendering (spec 007)

> **Tanggal**: 2026-08-22
> **Spec**: [`spec/specs/007.plan-v0.5.1-terminal-cwd-hotfix.md`](../specs/007.plan-v0.5.1-terminal-cwd-hotfix.md) → **Implemented**
> **Checklist**: [`spec/specs/007b.plan-checklist.md`](../specs/007b.plan-checklist.md) — 37 item `[x]`, 8 item tersisa = verifikasi manual di Termux (user-side)
> **Versi app**: `Cargo.toml` + `web/package.json` = **0.5.1**; `GET /api/v1/health` → `{"status":"ok","version":"0.5.1"}`

---

## 1. Yang Dikerjakan

### Milestone A — Backend (`V051-001`, `V051-005`)
- `src/session.rs:22-35` — `SessionMetadata` +field `work_dir: Option<PathBuf>` dengan
  `#[serde(skip_serializing_if = "Option::is_none")]`; diisi di `create_session`
  (`src/session.rs:163-183`, `work_dir.clone()` sebelum masuk `PtySession::new`).
- Tidak ada perubahan handler `create_terminal_handler` (`src/http.rs`) — nilai resolve
  EDT-002 kini ikut ter-serialisasi ke respons create. Wire format lama identik saat None.
- `Cargo.toml:3` + `web/package.json:4` bump `0.3.0` → `0.5.1`.
- Test baru: `tests/session_tests.rs` `test_v051_001_work_dir_in_create_resp`,
  `_work_dir_empty_is_config`, `_work_dir_root_is_reported`, `_traversal_no_work_dir`;
  `tests/boot_tests.rs` `test_v051_005_health_version_is_0_5_1`.
- Bukti RED→GREEN: RED awal 2 failed (session) + 1 failed (boot, versi 0.3.0); GREEN semua lulus.

### Milestone B — Frontend logic (`V051-002`, `V051-003`)
- `web/src/apps/editor/editorLogic.ts` — helper pure:
  - `displayWorkDir(workDir, ws)` → `{label, title, legacy}`; server path → basename,
    absen/kosong → fallback `${rootId}:${basePath||'/'}` dengan `legacy:true`.
  - `MIN_SERVER_VERSION = '0.5.1'`, `isVersionAtLeast()` (semver numeric-split, garbage=false-safe),
    `gateState(version, failed, dismissed)` → level/chip/banner matrix
    (stale→red+bad; unknown-version→yellow+warn; unreachable/pending→warn chip saja).
- `web/src/apps/editor/TextEditorView.tsx`:
  - state `terminalWorkDir` (dari `meta.work_dir` respons create; dibersihkan saat kill),
    `serverVersion`/`healthFailed`/`gateDismissed`.
  - Mount fetch `/api/v1/health` sekali (try/catch, cancel-safe).
  - Header terminal: `<span title={wdLabel.title}>💻 bash — {wdLabel.label}</span>` —
    tidak lagi menebak dari `ws` (fix inkonsistensi header ≠ pwd).
  - Banner merah `data-testid="version-gate-banner"` (backend < 0.5.1), banner kuning
    `cwd-legacy-banner` (spawn ok tapi server tidak mengonfirmasi cwd), chip status bar
    `data-testid="version-chip"` hijau/merah/kuning. Semua dismissible, `role="alert"`.
- Vitest: 14 test baru di `editorLogic.test.ts` (RED terbukti 14 failed dulu).

### Milestone C — Prompt rendering (`V051-004`)
- Dep baru: `@xterm/addon-unicode11 ^0.8.0` (`web/package.json`).
- `web/src/apps/terminal/terminalConfig.ts` (baru): `TERMINAL_FONT_STACK` +
  `applyTerminalAddons(term)` (loadAddon Unicode11 + `unicode.activeVersion='11'`).
- `web/src/apps/terminal/TerminalView.tsx` — pakai `TERMINAL_FONT_STACK` dan panggil
  `applyTerminalAddons(term)` tepat setelah `term.open()`.
- Font subset: `web/public/fonts/JetBrainsMonoNF-Mono-subset.woff2` (**42,432 bytes**)
  - source: `JetBrainsMonoNerdFontMono-Regular.ttf` nerd-fonts v3.2.1,
    sha256 `276cf05fa44937c906a34ad5f7473cddbc220dcd9f7d335afa7511a0c603f0f9`
  - subset sha256 `cbe7f107b987fd13e811a241945f7709ef1ba0d72721ec7978527560a8899df1`
  - ranges: `U+E0A0-E0D7` powerline, `U+E5FA-E6B3` FA-ext, `U+F400-F42B` octicons
    (\uf418 git branch starship), `U+2500-259F`, `U+2600-26FF`, `U+2700-27BF`
  - alat: `pyftsubset --flavor=woff2 --no-hinting` (brotli dipasang via pip).
- `web/src/styles/main.css` — `@font-face` dengan `unicode-range` persis subset,
  `font-display: swap`.

## 2. Bukti Gate (angka aktual)

| Gate | Hasil |
|---|---|
| `cargo test` | **125 passed / 0 failed** (120 regresi + 5 V051) |
| `cargo clippy --all-targets -- -D warnings` | 0 warning |
| `cargo fmt --check` | OK |
| `npm --prefix web test` | **38/38** (editorLogic 35 + pickerLogic + terminalConfig) |
| `npm run build` | JS **108.17 KiB gzip** + CSS 3.11 KiB + font 42 KiB ≤ 750 KiB |
| `scripts/check_budget.sh` | PASS (binary 2.74 MiB ≤50 MiB; frontend 108.17 KiB ≤750 KiB) |
| `tests/traceability.md` | `V051-001..006` → Passed; EDT-001..010 tetap Passed |
| graphify | `graphify update .` sinkron |

## 3. Fakta Kode Penting (untuk session berikutnya)

- Respons create kini: `{id, token, cols, rows, state, pid, work_dir?}` — `work_dir` hanya
  muncul saat cwd eksplisit di-resolve (payload legacy tanpa `cwd_*` dan config tanpa
  `--work-dir` → field ABSEN; test mengunci ini agar wire lama stabil).
- Label header editor sengaja menampilkan **basename** + absolute di `title`; reverse-map
  penuh absolute→relative ditunda (Q1 MVP decision, lihat checklist §6).
- `gateState(undefined)` = pending (fetch belum selesai) → warn chip tanpa banner;
  jangan ubah jadi red sebelum health sempat jawab.
- Font `unicode-range` harus persis sama dengan range pyftsubset; menambah glyph =
  subset ulang + update komentar checksum di `main.css`.
- `terminalConfig.test.ts` memakai stub semantik xterm (`loadAddon→activate`);
  JANGAN assert `constructor.name` (dimangle transform vitest → string kosong).

## 4. Pending (bloker nol, tapi wajib ditutup)

1. **Manual E2E di Termux nyata** (checklist 007b §4, item user-side):
   Open Folder `printing-web-app` → Ctrl+` → `pwd` = `/…/home/printing-web-app` →
   `git status` tanpa fatal → header konsisten → prompt tanpa kotak.
   Setelah lulus, centang 8 item itu di 007b.
2. **Jaminan install**: fresh `install.sh` di Termux bersih → `curl :PORT/api/v1/health`
   harus `"version":"0.5.1"` (release flow binary-first per AGENTS.md §9 — tag `v*` → CI build).
3. Release: tag `v0.5.1` saat mau rilis resmi.

## 5. Konteks untuk Session Berikutnya

- Root cause bug lapangan = version skew (RC-01): frontend v0.6 + backend tua diam-diam
  mengabaikan `cwd_*`. Kini terdeteksi: chip versi + banner merah. Kalau user masih
  melaporkan `pwd` salah SETELAH install 0.5.1, cek dulu apakah banner merah tampil
  (berarti binary belum ter-update, bukan regression).
- Multi-root workspace (tunda sejak 005 §8) tetap out-of-scope; Q1 reverse-map bisa
  naik prioritas kalau user minta label relative presisi.
