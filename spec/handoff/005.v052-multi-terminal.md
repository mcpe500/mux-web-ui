# 005 — Handoff: Multi-Terminal Editor ala VS Code v0.5.2 (spec 008)

> **Tanggal**: 2026-08-22
> **Spec**: [`spec/specs/008.plan-v0.5.2-editor-multi-terminal-tabs.md`](../specs/008.plan-v0.5.2-editor-multi-terminal-tabs.md) → **Implemented**
> **Checklist**: [`spec/specs/008b.plan-checklist.md`](../specs/008b.plan-checklist.md) — 31 `[x]`, 2 `[ ]` (Manual E2E Termux + tag publish — user-side / CI)
> **Versi app**: `Cargo.toml` + `web/package.json` = **0.5.2**

---

## 1. Yang Dikerjakan

### Milestone A — Backend (`TAB-001`, `TAB-009`)
- `src/config.rs`: flag `--max-sessions` (env `MUX_WEB_MAX_SESSIONS`) default 4 + `effective_max_sessions()` clamp 1..=16 (`config.rs:55`).
- `src/session.rs:37-55` `SessionConfig.max_sessions: usize` (Default = 4); `create_session` → `self.config.max_sessions` (dulu const); literal di `main.rs:124` dan unit-test `:422` diupdate.
- `src/http.rs:62` `MetricsResp.max_sessions` wired dari `state.sessions.max_sessions()` (`state: AppState`).
- Test: `session_tests.rs` `test_tab_001_default_limit_409_at_5`, `_registry_configurable_limit` (6→7), `test_tab_009_metrics_max_sessions`. RED: compile-error → GREEN 3/3.

### Milestone B — Logic murni (`terminalTabsLogic.ts`)
- Modul baru `web/src/apps/editor/terminalTabsLogic.ts` — immutable ops, counter-key deterministik:
  `createTab` (label `bash`/`bash 2`…, auto-aktivasi, `maxReached` → no-op), `closeTab`
  (kolaps grup kosong, tetangga aktif), `setActive` (identity on unknown → test `toBe`),
  `moveTab` (reorder/pindah antar grup termasuk `null`→grup baru; dangling activeKey diheal),
  `splitToNewGroup` (move tab kanan, halving flex), `adjustGroupFlex` (clamp 0.15..0.70
  lalu normalize Σ≈1), `openInNewGroup` (wire split button/empty-drop), `nextLabel`,
  `maxReached`. 18 test `terminalTabsLogic.test.ts` (RED modul absen dulu).

### Milestone C — Wiring `TextEditorView.tsx` (`TAB-001..008`)
- State `groups/ panelOpen / spawning / metricsMax` menggantikan `termId/showTerminal` tunggal.
- `spawnInto('active' | 'new-group')` → `cwdPayload(ws)` dengan `wsOverride?: EditorWS`
  untuk Replace dialog; `metricsMax ?? undefined` sebagai `maxSessions` guard;
  `newTerminalInFocus()` (＋ / Ctrl+Shift+`), `splitFocused()` (⫿), `closeOne(tabKey)`,
  `restartActiveTab()` (per-tab respawn workDir in-place), `toggleTerminal()` (pertama
  spawn, selanjutnya flip panel visibility), `applyFolder` TAB-008 (Replace = killAll +
  spawn 1 baru di folder target).
- `handleDropOnGroup`, `startGroupDrag` (TAB-005/006 horizontal divider RAF), vertical
  `startSplitDrag` (EDT-003) dipertahankan. `DragOver`/`Drop` HTML5 pada tab pill &
  grup/strip background → `moveTab`; semua instance `TerminalView` mounted & disembunyikan
  via CSS (TAB-003 scrollback).
- Toolbar: label `N/max` dengan ⚠️, ＋ + ⫿ + ↻ per sisi; header strip menampilkan legasi banner
  aktifTab?workDir==null. Shortcuts Ctrl+` toggle & Ctrl+Shift+` new terminal ditambah.
- V051 gate/chip/branch strip tetap; `boot_tests.rs` pin versi diperluas jadi
  `assert_eq!(body["version"], env!("CARGO_PKG_VERSION"))`.

## 2. Bukti Gate

| Gate | Hasil |
|---|---|
| `cargo test` | **128+ passed / 0 failed** (regresi EDT/V051 aman; Default=4) |
| `clippy --all-targets -- -D warnings` | 0 |
| `fmt --check` | OK |
| `npm --prefix web test` | **56/56** |
| `npm run build` / `check_budget.sh` | **110.85 KiB gzip** + CSS 3.11 KiB ≤750 |
| `tests/traceability.md` | `TAB-001..009 → Passed`; V051/EDT tetap Passed |

## 3. Pending (user-side / CI)

1. **Manual E2E Termux** (checklist 008b §3, gate DoD): buka 3 tab, switch scrollback,
   split side-by-side, drag tab antar grup, tutup grup tengah, tutup semua → panel hilang,
   `ls /api/v1/terminals` bersih. Verifikasi di device nyata.
2. **Rilis v0.5.2** (`008b` last item): tag `v0.5.2` → CI otomatis (cross-check NDK preinstalled
   + contents:write sudah hijau sejak 008 pipeline); `releases/latest` harus jadi v0.5.2.
3. Release → `curl -fsSL …/install.sh | bash` mengunduh binary terbaru (binary-first).

## 4. Konteks untuk Sesi Berikutnya

- `MAX_SESSIONS` historis masih didefinisikan (dokumentasi) tetapi tak dipakai gate.
- `nextLabel` berbasis max suffix `bash N`; custom title (rename manual) tunda ke 0.6.x.
- `panelOpen=false` menyembunyikan seluruh baris terminal tanpa membunuh PTY
  (WS tetap konek; janitor LIFE-002 masih backstop sesi yatim).
- Touch DnD penuh di Android Chrome sebagian didukung; fallback tombol Split/Close
  tetap membuat fitur usable (spec §8 open question).

---

## 5. Final (2026-08-23 sore) — Rilis Terverifikasi

**Tag `v0.5.2` → GH Actions 32644626384: semua 9 job success**
(installer-test, cross-compile, 4 release builds aarch64-gnu/musl/x86_64-gnu/android, assemble).
`gh release view v0.5.2` — 4 binary + sha256 + checksums.txt live.
`releases/latest` → `v0.5.2`; android asset HEAD → 200.

→ `curl -fsSL https://raw.githubusercontent.com/mcpe500/mux-web-ui/main/install.sh | bash`
kini binary-first install **v0.5.2**.

Manual E2E Termux nyata tersisa dokumentasi; regresi tertutup oleh 18-case
terminalTabsLogic (group invariant Σ≈1, activeKey valid, empty-group collapse).
