# Handoff 010 — v0.6.4 Documents & Agent Experience (Notebook / Python Runner / PDF Reader / Codex Window)

- **Spec**: [`014.plan-v0.6.4-documents-agents-experience`](../specs/014.plan-v0.6.4-documents-agents-experience.md) + [`014b.plan-checklist`](../specs/014b.plan-checklist.md)
- **Status**: Implemented (2026-08-27) — verifikasi independen QV/SV PASS (fix-loop closed)
- **Version**: `0.6.4` (`Cargo.toml:3`, `web/package.json:4`) — pre-bump SHA for retro-tag `v0.6.3` = `847e54f` (last push before bump; back-tag point for ceremony)
- **Baseline**: cargo 225/0 (205 + 20 baru), vitest 280/0 (140 + 140 baru), `tsc --noEmit --project web/tsconfig.json` clean, `clippy -D warnings` 0, `fmt --check` OK, `graphify-out` 2362 nodes/3727 edges
- **Budget**: `npm run build` 59 modules transformed — `index-_b0OElyj.js 517 KiB raw / 141 KiB gz` + `pdf-C5QHA24P.js 404 KiB / 118 KiB gz` + `pdf.worker.min-*.mjs 1,039 KiB / 279 KiB gz` + `tokenizer 1.37 KiB gz` + `css 3.2 KiB` → **Frontend Gzip 537 KiB** (`cat *.js *.mjs | gzip = 533 KiB` + css/html) **PASS ≤750** (soft 450 aspirational exceeded 87 KiB due to pinned `pdfjs-dist@5.4.149` — hard PASS, D-log). Binary gzip 2.96 MiB (6.65 MiB raw) PASS ≤50
- **Execution**: §11 DAG — WAVES 0→3 via 7 subagents (INV + 4B + QV/SV) + integrator; `perform-review → fix → re-verify` closed (Wave1 busy-slot + line-flood cap, budget mjs, etc)

## Milestone Evidence (file:line + bukti gate)

### Wave1 (847e54f) — PY + PDF core + AGX-red
- **M-B PY-001..006**: `src/run_tools.rs:32-48` `python_run_argv` table termux/distro, `src/run_tools.rs:57` TTL 30s `PROBE_TTL`, `src/run_tools.rs:155-343` `RunTaskRegistry` single-slot kind `"python"` bounded 16, capped pump `MAX_LINE_BYTES 256 KiB` truncate, `src/config.rs:112` `MUX_WEB_RUN_TIMEOUT_SECS` 300, routes `src/http.rs:117-122` inside auth chain, POST→400 PATH_TRAVERSAL / 409 TASK_BUSY with reap-before-busy `http.rs:596`, `reap_if_done` before `spawn_run` fixes wedge-after-no-WS (SV major). Frontend `TextEditorView.tsx:1080` gate `.endsWith('.py')` ▶, `RunPanel.tsx` WS `{line|exit}` + truncated marker `<…N lines truncated>` `http.rs:646`, `runPanelLogic.ts` last-frame traceback mapper 9 tests. Gates: `cargo test --test run_tools_tests 11` double-run 2.21s stable, vitest 9 pass — handoff harapkan.
- **M-C PDF-001..007 core**: `web/package.json:16` `pdfjs-dist 5.4.149` exact, `pdfLoader.ts:7` local `new URL('pdfjs-dist/build/pdf.worker.min.mjs',import.meta.url)` worker no CDN singleton memo, `PdfViewerView.tsx:87` `isEvalSupported:false` `disableAutoFetch:true`, `pdfLogic.ts:3` `DOC_MAX_BYTES 25 MiB` + `Content-Length` + mid-stream abort, 19 vitest `pdfLogic.test.ts` route/outline/search/loader PASS, viewer dead-code gap ditutup di Wave3 integrasi.
- **M-D AGX-red**: `codexEvents.ts:184` `>0.2` `bad*5>count` drift, `codexArgs.ts:24` `ALLOWED_SANDBOXES` allowlist default-deny + `validateCodexOpts` shell-meta `;[&|$\`` deny, fixtures `codexFixtures.ts` V1/V2 20+20 lines SYNTHETIC, 23+14+11 tests PASS.

### Wave2 — NB + AGX-wire
- **M-A NB-001..012**: `notebookModel.ts:5-8` guards `MAX_CELLS 2000` / `MAX_NOTEBOOK_BYTES 5 MiB` / `MAX_OUTPUT_BYTES 256 KiB` / `MAX_JSON_DEPTH 64` `depthExceeds` JSON bomb-lite before `JSON.parse` + `kind:'big'` collapse, `toLines` + v3 `worksheets→cells` migrate, canonical `serializeNotebook` stable order. `mdMini.ts:18` `sanitizeHref` http/https only else `#`, 0 `innerHTML` (comment-only), `RenderNode` tag whitelist 14 — XSS payloads `mdMini.test.ts` 13 PASS. `cellScript.ts:12` `MARK='# %% MUXCELL '` + `SKIP` stable, `splitRunStdout` trailing-into-current, mirror `notebook_exec.rs:133` `cell_script_body`. Backend L1 `src/notebook_exec.rs:28-55` `jupyter_run_argv` + `src/http.rs:734-842` `nb_execute_handler` `JUPYTER_MISSING` hint + `nb_executed_get_handler` purge-on-retrieval, `effective_run_timeout ≤600s` (`notebook_exec.rs:124`), WS `nb_ws_stream_socket` Lagged marker + 150ms poll, side-table LRU 32 (`notebook_exec.rs:146`). L2 `src/http.rs:848` hidden ` .mux_cell_<hex>.py` inside resolved parent + deferred unlink 1200ms grace kind-gated `python-cell` only. `NotebookView.tsx:828` tabs Origin/Hasil, virtualize-lite >120→±40, edit overlay `setCellSource`, save PUT `fs/file`, download `.py`, Kernel Bridge `mux_kernel_bridge` OFF default stub EXPERIMENTAL. Tests: `notebookModel.test.ts` 13 + `mdMini 13` + `cellScript 9` + `notebook_exec_tests` 9 pass double-run 4.6s stable — QV Wave2 PASS 275 vitest + 225 cargo.
- **M-D wire**: `transcriptBuffer.ts` chunk-aware `feed` buffering `\r` trim, `codexBridge.ts` `OutputPump` `TextDecoder streaming` + `composeLaunchInput` `posixQuote` escaping `\\ "$ \`` + `LAUNCH_MULTILINE_ERROR` `[\n\r]` reject, `keystrokesFor('y'|'n'|'esc')→'\n'/'\x1b'`, `shouldAutoSwitchToTerminal(report,burst)` `>0.2 || burst≥3`, `trimHistory 20×200`, `adapterTypes.ts` `AgentSurfaceAdapter` interface-only. `AgentCodexView.tsx:399` launcher fetches `/api/v1/environments` + `/api/v1/router/models` (`ROUTER_DOWN_TEXT`), single PTY surface toggle `display:none` (one socket `registerWriter`), drift toast `DRIFT_TOAST_TEXT` + 6s, composer + approval strip single channel, prompt library drawer `mux_codex_prompts_<sanitize>` flag ⚠, history + audit caps. Gates: vitest `codexBridge 15` `transcriptBuffer 9` → total 240 at Wave2 AGX-wire time, then 275 full. QV PASS.

### Wave3 — Integrasi desktop
- **windowStore `windowStore.ts:3`**: union now `... | 'notebooks' | 'pdf' | 'codex'` — **14 apps** (11→14)
- **windowRouter `windowRouter.ts:11-22`**: `TABLE [{ext:'.ipynb',appId:'notebooks'},{ext:'.pdf',appId:'pdf'}]` `routeForFile` `lower.endsWith(ext)` case-insensitive + `editor` fallback, tested `windowRouter.test.ts` 4 cases (incl `notebook.ipynb.bak→editor`) PASS
- **StartMenu `StartMenu.tsx:10-22`**: APPS 12 entries (9→12) icons 📓📄🤖 names `Notebooks`/`PDF Reader`/`Codex`
- **DesktopCanvas `DesktopCanvas.tsx:13-16,54-56,248-386`**: imports `NotebookView`/`PdfViewerView`/`AgentCodexView` + `routeForFile`, `openNotebooks/openPdf/openCodex` (ids `notebooks-${Date.now()}` etc, props `rootId/filePath`), `openApp` extends `else if notebooks/pdf/codex`, `FileExplorerView` wrapper `routeForFile(path)` routing `.ipynb→notebooks` `.pdf→pdf` else `editor` (defense still via `AllowedRoots::resolve_path`), render branches `{win.appId==='notebooks'&&<NotebookView>}` etc — `tsc web` clean, vitest 280
- **Support Hub `supportLogic.ts:85-166`**: GUIDES 8 entries (was 4→8) add `notebooks`/`python`/`pdf`/`codex` each 4 steps; updated `supportLogic.test.ts` superset check `toContain` + length≥4 + new 4 ids PASS (`14 tests`)
- **Version** `Cargo.toml:3` `0.6.4` + `web/package.json:4` `0.6.4` — health endpoint auto-matches `env!("CARGO_PKG_VERSION")`
- **Budget script** `scripts/check_budget.sh:31` fixed to count `*.js *.mjs` via `cat ... | gzip -c` (was missing `.mjs` worker undercount)
- **Build** `npm run build` 59 modules transformed, chunks above; `check_budget.sh` now 537 KiB gz hard PASS

## Threat Model Wiring (SV PASS)

- T1 argv-only: `src/run_tools.rs:41-46` `python3` literal + `src/notebook_exec.rs:44` jupyter argv literal + `src/http.rs:179` `Command::new(prog).args` no `sh -c`; probes `sh -lc "command -v ..."` fixed literal `src/run_tools.rs:92` `src/notebook_exec.rs:102` no interpolation; frontend `codexArgs.ts:44` `args.push('--json',prompt)` single element proven `codexArgs.test.ts:44` evil `; rm` stays one element
- T2 path: every input via `AllowedRoots::resolve_path` (`paths.rs:64` rejects `..`/NUL/prefix/symlink hardlink) — python `http.rs:555`, nb execute `729`, nb cell `837` + own temp `parent.join(.mux_cell_ hex)` not user join, fs GET/PUT `1767/1790`
- T3 probe cache TTL 30s per-tag (`run_tools.rs:57`, `notebook_exec.rs:65`) + `env_known` gate before spawn, fixed argv
- T4 caps: `MAX_NOTEBOOK_BYTES 5MiB`, `MAX_CELLS 2000`, `MAX_OUTPUT_BYTES 256 KiB→big`, `MAX_LINE_BYTES 256 KiB` truncate `TRUNC_MARKER`, `FINISHED_CAP 16` LRU, `PATH_TABLE_CAP 32`, `DOC_MAX_BYTES 25MiB` + stream abort, timeout 300s (python) / `min(600)` (notebook)
- T5 XSS: `isEvalSupported:false` + `disableAutoFetch:true`, local worker, 0 `innerHTML` outside comments, `sanitizeHref` only http/https
- T6 approvals: allowlist `read-only`/`workspace-write` else reject, denylist `--dangerously*/--yolo/--full-auto/-y/--yes`, `keystrokesFor` exact `y/n/esc` one-PTY forward no second spawn, prompt multiline `LAUNCH_MULTILINE_UNSAFE` rejected before spawn, replaced `[\n\r]`→space in composer
- T7 nbformat bomb: `depthExceeds >64` before parse, collapsed `big`, lazy image not executed (placeholder only)
- T8 routing: pure suffix table no FS bypass; actual fetch still validated
- T9 secrets not logged (`console.error('codex ...')` generic only), storage namespaced `mux_codex_prompts_` sanitized `[\w-]` else removed
- T10 middleware: 3 python routes `:117-122` + 4 notebook routes `:125-131` registered before `route_layer(auth)` `:187` + `csrf` + `host_origin`, auth covers `/api/v1/*` (`src/http.rs:1333`)
- MW 225/280 regressions zero; SV double-run detectors stable

## Smoke S9–S12 (handoff extension `spec/handoff/smoke-v0.6.3-device-checklist.md`)

Doc appended S9 notebooks (1–5 MiB <1.5s, edit→save→reload, Execute All/cell, bomb guard, Kernel Bridge OFF); S10 pdf textbook 10 MiB continuous `±2`, outline/thumb/goto/zoom/search, 25 MiB cap + fallback; S11 codex chat→tool cards→approval→library→drift >0.2 auto-terminal + history 20×200; S12 AFK streaming reconnect `500→10s` `canOpenSocket`. User-side execution pending device (log template in that file).

## Traceability 014b

Checklist `014b.plan-checklist.md` — all rows flipped `[x]` with gate evidence (test names + counts) per §11.4 LOLOS definisi; D-log: L3 Kernel Bridge stub flag-off (deliberate, not defect), NB-006 envcheck not a distinct route (covered by probe pre-check), budget `*.mjs` undercount fixed.

## Divergences / Follow-ups

- `notebookModel "big"` serializes to stub (`outputToNbformat big → <bytes collapsed>`) — exact giant-output byte-faithful lazy-output deferred Wave3 lazy work (doc note in `src/notebookModel.ts:204`). Not affecting gates.
- `check_budget.sh` kirim `Frontend Gzip 537 KiB` (hard 750 PASS, aspirational 450 exceed 87 KiB due to pinned `pdfjs-dist` + worker — soft exceed acceptable)
- `@xterm/addon-fit` + `lineStates.ts` dynamic import warning benign (static+dynamic same module — Vite note, not defect)
- Graph synced after hooks (`graphify update .` AST-only 2362 nodes); semantic update deferred (requires API key, per AGENTS.md next session)
- Tags pending: `v0.6.2 @2423885` + `v0.6.3 @847e54f` (pre-bump) → `v0.6.4` HEAD — ceremony blocked on public key paste (key stay `UNTRUSTED-PLACEHOLDER`), secret `MINISIGN_SIGNING_KEY` not yet set — rilis CI akan `⚠️ unsigned`

## Pre-bump SHA Capture

- Last commit pre-version bump (v0.6.3 code-complete): `847e54f feat(v064-wave1): python runner (PY), pdf reader core (PDF), codex event foundation (AGX-red) — TDD verified QV/SV`
- Tag `v0.6.3` must be created at `847e54f` (not at HEAD post-bump), and `v0.6.2` at `2423885` (last `0.6.2` commit)
