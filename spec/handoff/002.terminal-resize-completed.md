# Handoff 002 — Terminal Resizing & Window Control Hardening (v0.3)

Status: **Task Terminal Resize (v0.3) Selesai, Diuji TDD (78 tests Rust + Vitest hijau), & Pushed ke `main`.**
Tanggal: 2026-08-19
Companion: [`../specs/003.plan-v0.3-terminal-resize.md`](../specs/003.plan-v0.3-terminal-resize.md),
[`../specs/003b.plan-checklist.md`](../specs/003b.plan-checklist.md)

---

## 1. Status Ringkas

- **Spesifikasi & Checklist**: `spec/specs/003.plan-v0.3-terminal-resize.md` (status: `Implemented`) dan `spec/specs/003b.plan-checklist.md` (seluruh item `[x]`).
- **Requirement Family RSZ**: `RSZ-001` s/d `RSZ-010` ditambahkan dan ditandai `Passed` pada `tests/traceability.md`.
- **Backend PTY & HTTP Hardening**:
  - `src/pty.rs:65, 290, 365-385`: `MIN_COLS = 2`, `MAX_COLS = 500`, `MIN_ROWS = 1`, `MAX_ROWS = 200`, helper `clamp_dimensions()`, auto-clamping pada `new()` dan `resize()`.
  - `src/session.rs:171`: Auto-clamping pada pembuatan sesi `create_session()`.
  - `src/http.rs:322-355, 368, 658`: Pengecekan aman dan pelaporan `cols`/`rows` pada `TerminalListItem` dan WebSocket frame resize.
- **Frontend Window & Terminal**:
  - `web/src/desktop/WindowFrame.tsx`: 8-way resize handles (`n`, `s`, `e`, `w`, `ne`, `nw`, `se`, `sw`) dan bottom-right corner grip dengan Pointer Events (`minWidth: 240px`, `minHeight: 140px`).
  - `web/src/styles/main.css`: Styling dan kursor resize handle (`n-resize`, `s-resize`, `e-resize`, `w-resize`, `ne-resize`, `nw-resize`, `se-resize`, `sw-resize`).
  - `web/src/apps/terminal/TerminalView.tsx`: Debounce trailing-edge (~75ms) untuk mencegah frame flooding, proteksi `fitAddon.fit()` terhadap container `0x0`.
  - `web/src/desktop/windowStore.test.ts`: Vitest unit tests untuk reducers bounds.
- **Test Suite**:
  - `tests/resize_tests.rs`: 5 integration tests (`test_rsz_001` s/d `test_rsz_005`).
  - `cargo test`: 78 tests passed (0 failures).
  - `npm --prefix web test && npm --prefix web run build`: 3 passed, build clean.
  - `cargo clippy --all-targets -- -D warnings` & `cargo fmt --check`: Clean.

---

## 2. Git & Release Status

- Commit: `feat(terminal): implement interactive terminal resizing and dimension clamping`
- Branch `main` pushed to `https://github.com/mcpe500/mux-web-ui.git`.
