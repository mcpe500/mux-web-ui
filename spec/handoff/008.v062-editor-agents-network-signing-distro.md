# Handoff 008 — v0.6.2 Editor Polish & Virtualisasi Penuh, Agents UX, Persist Layout, Network Hardening, Signing, Distro Mgmt

> **Tanggal**: 2026-08-25
> **Spec**: [`011.plan-v0.6.2-editor-agents-network-signing-distro.md`](../specs/011.plan-v0.6.2-editor-agents-network-signing-distro.md)
> **Status**: KODE SELESAI + gates hijau; menunggu manual E2E user-side & key ceremony signing

## 1. Yang dikerjakan (semua milestone A–H)

| Area | Ringkasan |
|---|---|
| EDIT-017 | `tokenizeLineStateful(line, lang, inBlock)`; wrapper lama kompatibel (70 test lama tanpa edit). **Bonus: fix bug latent v0.6.1** — regex word inner `[A-Za-z0-9_$]` tanpa `#` membuat kata berawang `#` di json/md infinite-loop (`tokenizer.ts`). |
| EDIT-013 | `mux-code/brackets.ts` murni: auto-close/skip-over/wrap-seleksi/kutip-hug-guard; wire `handleKeyDown`. |
| EDIT-014 | Wrap toggle (toolbar+View+chip statusbar), persist `mux_editor_wrap_<winId>`, textarea & layer sinkron; gutter disembunyikan saat ON. |
| EDIT-018 | Virtualisasi penuh: `mux-code/lineStates.ts` (cache state inkremental diff prefix/suffix, recompute ekor saja), windowed gutter + layer `<pre>` (translateY/X rAF-throttled), textarea native utuh (caret/IME aman), cap highlight 200 KB → **2 MB**. Catatan: wrap ON mematikan overlay highlight sementara (keterbatasan terdokumentasi). |
| AGT-006 | Tombol 📁 + FolderPicker mode folder → chip `[cwd: root:path] ✕`; launch agent pakai wsOverride; backend resolve_path tetap otoritatif. Helper murni `mergeAgentSpawnPayload` (vitest). |
| TAB-010/011 | `serializeLayout/restoreLayout` (+storage helpers): hanya sessionId hidup dipulihkan via GET /terminals; JSON korup → kosong; clamp maxSessions; flex dinormalisasi; guard `layoutHydratedRef` agar state kosong tak menimpa layout tersimpan. |
| NET-007 | `--trusted-proxies`/env CSV ip/cidr fail-fast; `resolve_client_ip()` walk XFF kanan→kiri lewati trusted; dipakai rate-limit pairing; tanpa flag = byte-for-byte lama. |
| NET-008 | Wildcard `*.suffix` di `--allowed-hosts` (dot-boundary, multi-level); `*`/`*.` ditolak; warning startup trust-delegation; advertise tetap exact-only. **Perilaku berubah vs spec 009**: parser kini MENERIMA wildcard (test `net_006_parser_rejects_wildcard` diganti `net_006_parser_accepts_wildcard_in_allowed_hosts` + `net_008_advertise_still_rejects_wildcard`). |
| SIGN-001..003 | CI sign `checksums.txt` kondisional secret `MINISIGN_SIGNING_KEY`; `install.sh`/`update.sh` verifikasi minisign fail-closed sebelum sha256; fallback SHA-256-only + warning; `MUX_WEB_STRICT_VERIFY=1` fatal; pubkey override `MUX_WEB_MINISIGN_PUB` untuk test hermetic. Skenario lokal A–D lulus (verified/tamper-abort/strict-abort/sha256-only); CI scenario 5a/5b/5c ditambah. |
| DISTRO-001..004 | `src/distro_mgmt.rs`: katalog installed-vs-available (parser toleran), argv-only install/remove (id: regex + membership ganda), task registry single-slot (409 TASK_BUSY), WS stream line/exit, cancel kill; endpoint `/environments/catalog|:id/install|remove|tasks/:id(/stream)`; `install_hint` statis di registry agent (server TIDAK PERNAH mengeksekusi instalasi agent); UI modal ⚙️ dengan progress + konfirmasi ketik-id untuk remove. |

## 2. Keputusan implementasi (D-log)

| # | Keputusan |
|---|---|
| D-Q2 | `proot-distro remove` versi kini non-interaktif; runner tetap pipe `y\n` sebagai belt-and-braces. Cancel = SIGKILL langsung (tanpa SIGTERM→5s — deviasi minor dari spec, dicatat). |
| D-Q3 | Wildcard = multi-level subdomain (dot-boundary), suffix telanjang juga cocok (`*.ts.net` match `ts.net`) — konsisten praktik umum; warning startup mencantumkan entri. |
| D-virt | Wrap ON menyembunyikan gutter DAN overlay highlight (alignment visual rusak oleh wrap); teks tetap terbaca warna normal. |
| D-sign | Secret `MINISIGN_SIGNING_KEY` harus berisi private key TANPA passphrase; sampai diset, rilis terbit unsigned dengan `::warning::`. |

## 3. Gates (semua hijau)

- `cargo test` — 172 test / semua suite PASS (regresi nol; satu penyesuaian semantik NET-006 terdokumentasi di atas)
- `cargo clippy --all-targets -- -D warnings` — 0 · `cargo fmt --check` — 0
- vitest — **117/117** · `tsc --noEmit` — bersih
- `scripts/check_budget.sh` — PASS (frontend gzip **113.53 KiB** ≤ 750 KiB)
- `graphify update .` — tersinkron (1635 node / 2827 edge)
- Versi → **0.6.2** (Cargo.toml + web/package.json)

## 4. Pending (user-side)

1. **Manual E2E Termux+proot**: quick-launch opencode lewat cwd picker; **verifikasi binary `agy` (Q1)** — bila salah ubah satu baris di `src/agents.rs` registry; screen-off 10 mnt; file 10k baris; install/remove distro roundtrip; spoof XFF matrix; update.sh signature.
2. **Key ceremony minisign**: `minisign -G -p share/minisign.pub -s minisign.key` (offline) → commit pubkey baru + set secret GitHub `MINISIGN_SIGNING_KEY` (isi file key tanpa passphrase) → embed pubkey ke kedua script (ganti marker UNTRUSTED-PLACEHOLDER).
3. CI run pertama setelah push: pastikan job `audit`, scenario 5a–5c, dan release signing jalan.
